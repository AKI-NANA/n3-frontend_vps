{
  "name": "N3-04-価格計算-AI補完DDP計算-V5-PRODUCTION",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "enrich-batch",
        "options": {
          "responseMode": "responseNode"
        }
      },
      "id": "webhook",
      "name": "Webhook: 価格計算依頼",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "webhookId": "enrich-batch"
    },
    {
      "parameters": {
        "jsCode": "// V5-PRODUCTION: 内部トークン検証（crypto依存排除版）\n// ========================================\nconst body = $input.first().json.body || $input.first().json;\nconst headers = $input.first().json.headers || {};\n\n// 環境変数必須チェック\nconst apiUrl = $env.N3_API_URL;\nconst secret = $env.N8N_INTERNAL_SECRET;\nif (!apiUrl) {\n  return [{ json: { error: true, message: 'N3_API_URL 環境変数が未設定です', code: 'CONFIG_ERROR' } }];\n}\nif (!secret) {\n  return [{ json: { error: true, message: 'N8N_INTERNAL_SECRET 環境変数が未設定です', code: 'CONFIG_ERROR' } }];\n}\n\nconst internalToken = headers['x-n3-signature'] || headers['X-N3-Signature'];\nconst tokenTimestamp = headers['x-n3-token-timestamp'] || headers['X-N3-Token-Timestamp'];\nconst userId = body.user_id || 'default';\n\n// トークン検証（外部APIへ委譲）\nif (internalToken && tokenTimestamp) {\n  try {\n    const verifyResponse = await fetch(`${apiUrl}/api/n8n-auth/verify-internal-token`, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ userId, timestamp: tokenTimestamp, token: internalToken })\n    });\n    const verifyResult = await verifyResponse.json();\n    \n    if (!verifyResult.valid) {\n      return [{ json: { error: true, message: '内部トークンが無効です', code: 'INVALID_TOKEN' } }];\n    }\n  } catch (e) {\n    return [{ json: { error: true, message: 'トークン検証APIエラー: ' + e.message, code: 'VERIFY_API_ERROR' } }];\n  }\n}\n\n// 入力検証\nconst productId = body.product_id;\nconst marketplace = body.marketplace || 'ebay_us';\nconst limit = body.limit || 10;\nconst force = body.force || false;\n\nreturn [{ json: { \n  product_id: productId, \n  marketplace: marketplace, \n  user_id: userId,\n  limit: limit,\n  force: force,\n  request_timestamp: new Date().toISOString()\n} }];"
      },
      "id": "validate",
      "name": "リクエスト検証",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{ $json.error }}",
              "value2": true
            }
          ]
        }
      },
      "id": "check-error",
      "name": "エラーチェック",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ success: false, error: $json.message }) }}",
        "options": {
          "responseCode": 403
        }
      },
      "id": "respond-error",
      "name": "エラー応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT p.id, p.title, p.english_title, p.sku, p.purchase_price, p.cost_price, p.weight_g, p.hts_code, p.origin_country, p.ebay_category_id, p.sm_lowest_price, p.sm_average_price, p.ebay_api_data, p.listing_data, p.sm_selected_item, p.condition, p.workflow_status, COALESCE(p.manual_override, '{}'::jsonb) as manual_override, p.ai_confidence_score, p.user_id, p.updated_at FROM products_master p WHERE p.workflow_status = 'enrich' AND p.user_id = $1 ORDER BY p.updated_at DESC LIMIT $2",
        "options": {
          "queryParams": "={{ [{{ $json.user_id }}, {{ $json.limit }}] }}"
        }
      },
      "id": "get-products",
      "name": "補完待ち取得（user_id検証）",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      }
    },
    {
      "parameters": {
        "conditions": {
          "number": [
            {
              "value1": "={{ Array.isArray($json) ? $json.length : ($json.id ? 1 : 0) }}",
              "operation": "equal",
              "value2": 0
            }
          ]
        }
      },
      "id": "check-empty",
      "name": "対象有無",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "jsCode": "return [{ json: { success: true, processed: 0, message: '補完待ち商品なし' } }];"
      },
      "id": "no-items",
      "name": "対象なし",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT gs.key, gs.value FROM global_settings gs WHERE gs.user_id = $1 AND gs.key IN ('exchange_rate_usd_jpy', 'target_margin', 'fvf_rate', 'min_margin', 'ddp_service_fee')",
        "options": {
          "queryParams": "={{ [{{ $node[\"リクエスト検証\"].json.user_id }}] }}"
        }
      },
      "id": "get-config",
      "name": "計算設定取得（user_id検証）",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT hts_code, hts_prefix, product_type, category_keywords, ebay_category_ids, base_tariff_rate, section_301_applicable, section_301_rate, description_ja, confidence_level FROM hts_rates WHERE hts_code != '9999.99.99' ORDER BY confidence_level = 'high' DESC, product_type LIMIT 500",
        "options": {}
      },
      "id": "get-hts",
      "name": "HTSマスタ取得",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// V4-PRODUCTION: データ準備 + HTSマスタ照合 + 手動編集保護\nconst products = $node['補完待ち取得（user_id検証）'].json;\nconst productArr = Array.isArray(products) ? products : [products];\nconst configArr = Array.isArray($node['計算設定取得（user_id検証）'].json) ? $node['計算設定取得（user_id検証）'].json : [$node['計算設定取得（user_id検証）'].json];\nconst htsArr = Array.isArray($input.first().json) ? $input.first().json : [$input.first().json];\n\nconst config = { exchange_rate: 150, target_margin: 15, fvf_rate: 12.95, min_margin: 5, ddp_service_fee: 15 };\nfor (const row of configArr) {\n  if (!row.key) continue;\n  if (row.key === 'exchange_rate_usd_jpy') config.exchange_rate = parseFloat(row.value) || 150;\n  else config[row.key] = parseFloat(row.value);\n}\n\nconst htsMaster = htsArr.filter(h => h.hts_code);\n\nfunction searchHTS(title, ebayCategory) {\n  const titleLower = (title || '').toLowerCase();\n  const categoryId = parseInt(ebayCategory) || null;\n  if (categoryId) {\n    const catMatch = htsMaster.find(h => h.ebay_category_ids && h.ebay_category_ids.includes(categoryId) && h.confidence_level === 'high');\n    if (catMatch) return { ...catMatch, match_type: 'category_exact', confidence: 95 };\n  }\n  for (const h of htsMaster) {\n    if (!h.category_keywords || h.category_keywords.length === 0) continue;\n    const matchedKW = h.category_keywords.filter(kw => titleLower.includes(kw.toLowerCase()));\n    if (matchedKW.length > 0) {\n      const conf = Math.min(90, 70 + matchedKW.length * 5);\n      return { ...h, match_type: 'keyword_match', matched_keywords: matchedKW, confidence: h.confidence_level === 'high' ? conf : conf - 10 };\n    }\n  }\n  return null;\n}\n\nreturn productArr.filter(p => p.id).map(p => {\n  let listingData = {}, manualOverride = {};\n  try { listingData = typeof p.listing_data === 'string' ? JSON.parse(p.listing_data || '{}') : (p.listing_data || {}); } catch (e) {}\n  try { manualOverride = typeof p.manual_override === 'string' ? JSON.parse(p.manual_override || '{}') : (p.manual_override || {}); } catch (e) {}\n  \n  const htsMatch = searchHTS(p.english_title || p.title, p.ebay_category_id);\n  const isManualHTS = manualOverride.hts_code === true;\n  const isManualWeight = manualOverride.weight_g === true;\n  \n  let finalHTS = { hts_code: null, tariff_rate: 0.06, source: 'pending_ai', confidence: 0 };\n  if (isManualHTS && p.hts_code) {\n    finalHTS = { hts_code: p.hts_code, tariff_rate: 0.06, source: 'manual_entry', confidence: 100 };\n    const manualMatch = htsMaster.find(h => h.hts_code === p.hts_code);\n    if (manualMatch) finalHTS.tariff_rate = manualMatch.base_tariff_rate || 0;\n  } else if (htsMatch) {\n    finalHTS = { hts_code: htsMatch.hts_code, tariff_rate: htsMatch.base_tariff_rate || 0, source: 'master_match', confidence: htsMatch.confidence };\n  } else if (p.hts_code) {\n    finalHTS = { hts_code: p.hts_code, tariff_rate: 0.06, source: 'existing', confidence: 60 };\n  }\n  \n  const finalWeight = isManualWeight && p.weight_g ? p.weight_g : (p.weight_g || listingData.weight_g || 500);\n  const finalOrigin = p.origin_country || 'JP';\n  const needsAI = finalHTS.source === 'pending_ai' || !listingData.item_specifics;\n  \n  return { json: {\n    user_id: p.user_id, product_id: p.id, sku: p.sku, updated_at: p.updated_at,\n    title: p.english_title || p.title, cost_jpy: p.purchase_price || p.cost_price || 0,\n    final_hts: finalHTS, final_weight_g: finalWeight, final_origin: finalOrigin,\n    manual_override: manualOverride, needs_ai: needsAI,\n    ebay_category_id: p.ebay_category_id || '', sm_lowest_price: p.sm_lowest_price || 0, sm_average_price: p.sm_average_price || 0,\n    item_specifics: listingData.item_specifics || {},\n    exchange_rate: config.exchange_rate, config\n  } };\n});"
      },
      "id": "prepare",
      "name": "データ準備（HTSマスタ照合）",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "batchSize": 3,
        "options": {}
      },
      "id": "batch",
      "name": "3件ずつ処理",
      "type": "n8n-nodes-base.splitInBatches",
      "typeVersion": 3
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{ $json.needs_ai }}",
              "value2": true
            }
          ]
        }
      },
      "id": "check-ai",
      "name": "AI必要？",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "jsCode": "const p = $input.first().json;\nreturn [{ json: { ...p, ai_skipped: true, confidence_score: p.final_hts.confidence || 100, enrichment_source: p.final_hts.source || 'manual_entry' } }];"
      },
      "id": "skip-ai",
      "name": "AIスキップ",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.N3_API_URL || '{{$env.GATEWAY_URL}}' }}/api/ai/analyze",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ model: 'gemini-1.5-flash', prompt: '商品「' + $json.title + '」のHSコード、原産国、推定重量、ItemSpecificsを推定して。\\nカテゴリID: ' + $json.ebay_category_id + '\\n形式: {\"hts_code\":\"9504.40.00\",\"origin_country\":\"JP\",\"weight_g\":500,\"confidence\":85,\"item_specifics\":{\"Brand\":\"...\",\"MPN\":\"Does Not Apply\"}}' }) }}",
        "options": {
          "timeout": 30000
        }
      },
      "id": "ai-enrich",
      "name": "Gemini: AI補完",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "// V4-PRODUCTION: AI応答パース + DDP価格計算（圧縮）\nlet ai = {};\ntry {\n  const text = $input.first().json.text || $input.first().json.response || '{}';\n  const jsonMatch = text.match(/\\{[\\s\\S]*\\}/);\n  if (jsonMatch) ai = JSON.parse(jsonMatch[0]);\n} catch (e) { ai = { confidence: 50 }; }\n\nconst p = $node['3件ずつ処理'].json;\nconst config = p.config || { target_margin: 15, fvf_rate: 12.95, ddp_service_fee: 15 };\n\n// HTS/重量/原産国の決定（手動優先）\nlet htsCode = p.final_hts.hts_code, htsSource = p.final_hts.source, confidence = p.final_hts.confidence;\nif (p.final_hts.source === 'pending_ai' && ai.hts_code) { htsCode = ai.hts_code; htsSource = 'ai_inference'; confidence = ai.confidence || 70; }\nelse if (!htsCode) { htsCode = '9504.40.00'; htsSource = 'default'; confidence = 50; }\n\nlet weightG = p.final_weight_g;\nif (weightG === 500 && ai.weight_g) weightG = ai.weight_g;\n\nlet origin = p.final_origin;\nif (!origin && ai.origin_country) origin = ai.origin_country;\nif (!origin) origin = 'JP';\n\nconst itemSpecifics = { ...(p.item_specifics || {}), ...(ai.item_specifics || {}) };\nif (!itemSpecifics.Brand) itemSpecifics.Brand = 'Unbranded';\nif (!itemSpecifics.MPN) itemSpecifics.MPN = 'Does Not Apply';\n\n// DDP価格計算\nconst exchangeRate = p.exchange_rate || 150;\nconst costUSD = (p.cost_jpy || 0) / exchangeRate;\nconst weightKg = weightG / 1000;\nconst targetMargin = (config.target_margin || 15) / 100;\nconst fvfRate = (config.fvf_rate || 12.95) / 100;\n\nconst TARIFF_RATES = { '9504.40': 0, '9503.00': 0, '8471.30': 0, '8517.12': 0 };\nconst htsPrefix = (htsCode || '').substring(0, 7);\nlet baseTariff = TARIFF_RATES[htsPrefix] ?? 0.06;\nlet additionalTariff = origin === 'CN' ? 0.25 : 0;\nconst totalTariffRate = baseTariff + additionalTariff;\n\nconst SHIPPING = [{ maxKg: 0.5, base: 12, total: 15 }, { maxKg: 1.0, base: 15, total: 20 }, { maxKg: 2.0, base: 20, total: 28 }, { maxKg: 5.0, base: 35, total: 45 }, { maxKg: 10.0, base: 55, total: 70 }];\nconst policy = SHIPPING.find(s => weightKg <= s.maxKg) || SHIPPING[SHIPPING.length - 1];\nconst baseShipping = policy.base, totalShipping = policy.total;\n\nconst variableRate = fvfRate + 0.02 + 0.03 + 0.015;\nconst DDP_FEE = config.ddp_service_fee || 15;\nconst insertionFee = 0.35;\n\nlet productPrice = 50;\nfor (let i = 0; i < 10; i++) {\n  const tariff = productPrice * totalTariffRate;\n  const salesTax = productPrice * 0.08;\n  const mpf = productPrice * 0.003464;\n  const ddpCost = tariff + salesTax + mpf + DDP_FEE;\n  const fixedCost = costUSD + baseShipping + ddpCost + insertionFee;\n  const requiredRevenue = fixedCost / (1 - targetMargin - variableRate);\n  const newPrice = requiredRevenue - totalShipping;\n  if (Math.abs(newPrice - productPrice) < 0.1) { productPrice = newPrice; break; }\n  productPrice = newPrice;\n}\nproductPrice = Math.max(10, Math.round(productPrice / 5) * 5);\n\nif (p.sm_lowest_price > 0 && productPrice < p.sm_lowest_price * 0.8) productPrice = Math.round(p.sm_lowest_price * 0.9 / 5) * 5;\nif (p.sm_average_price > 0 && productPrice > p.sm_average_price * 1.3) productPrice = Math.round(p.sm_average_price * 1.2 / 5) * 5;\n\nconst totalRevenue = productPrice + totalShipping;\nconst tariff = productPrice * totalTariffRate;\nconst ddpTotal = tariff + productPrice * 0.08 + productPrice * 0.003464 + DDP_FEE;\nconst ebayFees = totalRevenue * (fvfRate + 0.02 + 0.03 + 0.015) + insertionFee;\nconst totalCosts = costUSD + baseShipping + ddpTotal + ebayFees;\nconst profit = totalRevenue - totalCosts;\nconst profitMargin = totalRevenue > 0 ? (profit / totalRevenue) * 100 : 0;\n\nconst hasTariff = totalTariffRate > 0;\nconst requiredMargin = hasTariff ? 10 : 5;\nlet isRedFlag = false, workflowStatus = 'ready', errorReason = '';\nif (confidence < 80) { workflowStatus = 'review'; isRedFlag = true; errorReason = '信頼度不足:' + confidence; }\nelse if (profit < 0) { workflowStatus = 'review'; isRedFlag = true; errorReason = '赤字:$' + profit.toFixed(2); }\nelse if (profitMargin < requiredMargin) { workflowStatus = 'review'; isRedFlag = true; errorReason = '利益率不足:' + profitMargin.toFixed(1) + '%'; }\n\nreturn [{ json: {\n  user_id: p.user_id, product_id: p.product_id, sku: p.sku, updated_at: p.updated_at,\n  hts_code: htsCode, hts_source: htsSource, origin_country: origin, weight_g: weightG,\n  item_specifics: itemSpecifics, confidence_score: confidence,\n  price_usd: productPrice, shipping_usd: totalShipping, profit_usd: Math.round(profit * 100) / 100,\n  profit_margin: Math.round(profitMargin * 10) / 10, profit_jpy: Math.round(profit * exchangeRate),\n  workflow_status: workflowStatus, is_red_flag: isRedFlag, error_reason: errorReason\n} }];"
      },
      "id": "calc-ddp",
      "name": "DDP価格計算（圧縮）",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "UPDATE products_master SET hts_code = COALESCE(NULLIF($1, ''), hts_code), origin_country = COALESCE(NULLIF($2, ''), origin_country), weight_g = $3, listing_data = COALESCE(listing_data, '{}'::jsonb) || '{{ JSON.stringify({ is: $json.item_specifics, hs: $json.hts_source }).replace(/'/g, \"''\") }}'::jsonb, ddp_price_usd = $4, price_usd = $5, shipping_cost_usd = $6, profit_usd = $7, profit_margin = $8, profit_jpy = $9, workflow_status = $10, ai_confidence_score = $11, updated_at = NOW() WHERE id = $12 AND user_id = $13 AND updated_at = $14 RETURNING id, sku, workflow_status",
        "options": {
          "queryParams": "={{ [{{ $json.hts_code }}, {{ $json.origin_country }}, {{ $json.weight_g }}, {{ $json.price_usd }}, {{ $json.price_usd }}, {{ $json.shipping_usd }}, {{ $json.profit_usd }}, {{ $json.profit_margin }}, {{ $json.profit_jpy }}, {{ $json.workflow_status }}, {{ $json.confidence_score }}, {{ $json.product_id }}, {{ $json.user_id }}, {{ $json.updated_at }}] }}"
        }
      },
      "id": "save",
      "name": "計算結果保存（楽観的ロック）",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      },
      "continueOnFail": true
    },
    {
      "parameters": {
        "amount": 1,
        "unit": "seconds"
      },
      "id": "wait",
      "name": "Wait",
      "type": "n8n-nodes-base.wait",
      "typeVersion": 1.1
    },
    {
      "parameters": {
        "jsCode": "const results = $input.all().map(i => i.json);\nconst ready = results.filter(r => r.workflow_status === 'ready').length;\nconst review = results.filter(r => r.workflow_status === 'review').length;\nconst avgMargin = results.length > 0 ? results.reduce((sum, r) => sum + (r.profit_margin || 0), 0) / results.length : 0;\nreturn [{ json: { success: true, processed: results.length, ready, review, avg_margin: Math.round(avgMargin * 10) / 10, ts: new Date().toISOString() } }];"
      },
      "id": "aggregate",
      "name": "結果集約",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "DELETE FROM ai_enrichment_logs WHERE created_at < NOW() - INTERVAL '7 days'",
        "options": {}
      },
      "id": "purge",
      "name": "🗑️ 履歴パージ",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      },
      "continueOnFail": true
    },
    {
      "parameters": {
        "conditions": {
          "number": [
            {
              "value1": "={{ $node['結果集約'].json.review }}",
              "operation": "larger",
              "value2": 0
            }
          ]
        }
      },
      "id": "check-alert",
      "name": "要確認あり？",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.chatwork.com/v2/rooms/{{ $env.CHATWORK_ROOM_ID }}/messages",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-ChatWorkToken",
              "value": "={{ $env.CHATWORK_API_KEY }}"
            }
          ]
        },
        "sendBody": true,
        "bodyParameters": {
          "parameters": [
            {
              "name": "body",
              "value": "=[info][title]📊 N3-04 V4 価格計算完了[/title]処理: {{ $node['結果集約'].json.processed }}件\\n✅出品可能: {{ $node['結果集約'].json.ready }}件\\n⚠️要確認: {{ $node['結果集約'].json.review }}件\\n平均利益率: {{ $node['結果集約'].json.avg_margin }}%[/info]"
            }
          ]
        }
      },
      "id": "notify",
      "name": "ChatWork通知",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($node['結果集約'].json) }}",
        "options": {
          "responseCode": 200
        }
      },
      "id": "respond",
      "name": "応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1
    },
    {
      "parameters": {
        "jsCode": "// 🔐 HMAC SHA256 署名検証 (Empire OS v6)\nconst crypto = require('crypto');\n\nconst signature = $request.headers['x-n3-signature'] || $request.headers['X-N3-Signature'];\nconst timestamp = $request.headers['x-n3-timestamp'] || $request.headers['X-N3-Timestamp'];\nconst secret = $env.N8N_HMAC_SECRET;\n\nif (!secret) {\n  throw new Error('🚫 N8N_HMAC_SECRET 環境変数が未設定です');\n}\n\nif (!signature || !timestamp) {\n  throw new Error('🚫 署名またはタイムスタンプが欠落しています');\n}\n\n// タイムスタンプ有効期限チェック (5分)\nconst now = Date.now();\nconst tsAge = now - parseInt(timestamp);\nif (tsAge > 300000 || tsAge < -30000) {\n  throw new Error('🚫 タイムスタンプが期限切れまたは不正です');\n}\n\n// HMAC検証\nconst bodyString = JSON.stringify($json);\nconst computedHmac = crypto\n  .createHmac('sha256', secret)\n  .update(timestamp + '.' + bodyString)\n  .digest('hex');\n\nif (signature !== computedHmac) {\n  throw new Error('🚫 HMAC署名検証失敗: 不正なリクエスト');\n}\n\n// 検証成功 - 元データをそのまま返す\nreturn [{ json: $json }];"
      },
      "name": "🔐 HMAC署名検証",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[EMPIRE_OS_V6] セキュリティ強制挿入"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{$env.SUPABASE_URL}}/rest/v1/execution_logs",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "apikey",
              "value": "={{$env.SUPABASE_ANON_KEY}}"
            },
            {
              "name": "Authorization",
              "value": "=Bearer {{$env.SUPABASE_SERVICE_ROLE_KEY}}"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Prefer",
              "value": "return=minimal"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\n  \"workflow_id\": \"{{$workflow.id}}\",\n  \"workflow_name\": \"{{$workflow.name}}\",\n  \"status\": \"success\",\n  \"execution_time_ms\": {{Date.now() - $execution.startedAt.getTime()}},\n  \"error_message\": null,\n  \"executed_at\": \"{{new Date().toISOString()}}\",\n  \"node_count\": {{$workflow.nodes.length}},\n  \"metadata\": {}\n}"
      },
      "name": "📊 実行ログ送信 (成功)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true,
      "notes": "[EMPIRE_OS_V6] 中央監視パルス"
    }
  ],
  "connections": {
    "Webhook: 価格計算依頼": {
      "main": [
        [
          {
            "node": "🔐 HMAC署名検証",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "リクエスト検証": {
      "main": [
        [
          {
            "node": "エラーチェック",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "エラーチェック": {
      "main": [
        [
          {
            "node": "エラー応答",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "補完待ち取得（user_id検証）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "補完待ち取得（user_id検証）": {
      "main": [
        [
          {
            "node": "対象有無",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "対象有無": {
      "main": [
        [
          {
            "node": "対象なし",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "計算設定取得（user_id検証）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "対象なし": {
      "main": [
        [
          {
            "node": "応答",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "計算設定取得（user_id検証）": {
      "main": [
        [
          {
            "node": "HTSマスタ取得",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "HTSマスタ取得": {
      "main": [
        [
          {
            "node": "データ準備（HTSマスタ照合）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "データ準備（HTSマスタ照合）": {
      "main": [
        [
          {
            "node": "3件ずつ処理",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "3件ずつ処理": {
      "main": [
        [
          {
            "node": "AI必要？",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "結果集約",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "AI必要？": {
      "main": [
        [
          {
            "node": "Gemini: AI補完",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "AIスキップ",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Gemini: AI補完": {
      "main": [
        [
          {
            "node": "DDP価格計算（圧縮）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "AIスキップ": {
      "main": [
        [
          {
            "node": "DDP価格計算（圧縮）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "DDP価格計算（圧縮）": {
      "main": [
        [
          {
            "node": "計算結果保存（楽観的ロック）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "計算結果保存（楽観的ロック）": {
      "main": [
        [
          {
            "node": "Wait",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Wait": {
      "main": [
        [
          {
            "node": "3件ずつ処理",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "結果集約": {
      "main": [
        [
          {
            "node": "🗑️ 履歴パージ",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🗑️ 履歴パージ": {
      "main": [
        [
          {
            "node": "要確認あり？",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "要確認あり？": {
      "main": [
        [
          {
            "node": "ChatWork通知",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "応答",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "ChatWork通知": {
      "main": [
        [
          {
            "node": "応答",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🔐 HMAC署名検証": {
      "main": [
        [
          {
            "node": "リクエスト検証",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionTimeout": 600,
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "none"
  },
  "tags": [
    {
      "name": "N3-Price",
      "color": "#32cd32"
    },
    {
      "name": "V4-PRODUCTION",
      "color": "#ff0000"
    },
    {
      "name": "AI-Powered",
      "color": "#9932cc"
    },
    {
      "name": "Multi-Tenant",
      "color": "#00bfff"
    },
    {
      "name": "Internal-Auth",
      "color": "#dc143c"
    },
    {
      "name": "500MB-Safe",
      "color": "#32cd32"
    },
    {
      "name": "Empire-OS-V6",
      "color": "#ff4500"
    }
  ],
  "active": false
}