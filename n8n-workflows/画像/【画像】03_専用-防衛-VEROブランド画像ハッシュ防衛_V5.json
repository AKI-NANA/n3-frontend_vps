{
  "name": "【画像】03_専用-防衛-VEROブランド画像ハッシュ防衛_V5",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "vero-shield-check",
        "options": {
          "responseMode": "responseNode"
        }
      },
      "id": "webhook",
      "name": "Webhook: 出品前チェック依頼",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "webhookId": "vero-shield-check"
    },
    {
      "parameters": {
        "jsCode": "// V5-PRODUCTION: 内部トークン検証（crypto依存排除版）\n// ========================================\nconst body = $input.first().json.body || $input.first().json;\nconst headers = $input.first().json.headers || {};\n\n// 環境変数必須チェック\nconst apiUrl = $env.N3_API_URL;\nconst secret = $env.N8N_INTERNAL_SECRET;\nif (!apiUrl) {\n  return [{ json: { error: true, message: 'N3_API_URL 環境変数が未設定です', code: 'CONFIG_ERROR' } }];\n}\nif (!secret) {\n  return [{ json: { error: true, message: 'N8N_INTERNAL_SECRET 環境変数が未設定です', code: 'CONFIG_ERROR' } }];\n}\n\nconst internalToken = headers['x-n3-signature'] || headers['X-N3-Signature'];\nconst tokenTimestamp = headers['x-n3-token-timestamp'] || headers['X-N3-Token-Timestamp'];\nconst userId = body.user_id || 'default';\n\n// トークン検証（外部APIへ委譲）\nif (internalToken && tokenTimestamp) {\n  try {\n    const verifyResponse = await fetch(`${apiUrl}/api/n8n-auth/verify-internal-token`, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ userId, timestamp: tokenTimestamp, token: internalToken })\n    });\n    const verifyResult = await verifyResponse.json();\n    \n    if (!verifyResult.valid) {\n      return [{ json: { error: true, message: '内部トークンが無効です', code: 'INVALID_TOKEN' } }];\n    }\n  } catch (e) {\n    return [{ json: { error: true, message: 'トークン検証APIエラー: ' + e.message, code: 'VERIFY_API_ERROR' } }];\n  }\n}\n\n// 入力検証\nconst productId = body.product_id;\nconst marketplace = body.marketplace || 'ebay_us';\nconst limit = body.limit || 10;\nconst force = body.force || false;\n\nreturn [{ json: { \n  product_id: productId, \n  marketplace: marketplace, \n  user_id: userId,\n  limit: limit,\n  force: force,\n  request_timestamp: new Date().toISOString()\n} }];"
      },
      "id": "validate",
      "name": "リクエスト検証",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{ $json.error }}",
              "value2": true
            }
          ]
        }
      },
      "id": "check-error",
      "name": "エラーチェック",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ success: false, error: $json.message }) }}",
        "options": {
          "responseCode": 403
        }
      },
      "id": "respond-error",
      "name": "エラー応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT vb.id, vb.brand_name, vb.risk_level, vb.vero_keywords, vb.image_hash_patterns FROM vero_brands vb WHERE vb.user_id = $1 OR vb.user_id IS NULL ORDER BY vb.risk_level DESC",
        "options": {
          "queryParams": "={{ [{{ $json.user_id }}] }}"
        }
      },
      "id": "get-vero-brands",
      "name": "禁止ブランドリスト取得（user_id検証）",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// V4-PRODUCTION: VEROブランド照合エンジン\nconst request = $node['リクエスト検証'].json;\nconst veroBrands = Array.isArray($input.first().json) ? $input.first().json : [$input.first().json];\n\nconst brand = (request.brand || '').toLowerCase();\nconst title = (request.title || '').toLowerCase();\n\nlet isBlocked = false;\nlet matchedBrand = null;\nlet riskLevel = 'low';\nlet blockReason = '';\n\nfor (const vero of veroBrands) {\n  if (!vero.brand_name) continue;\n  const veroBrand = vero.brand_name.toLowerCase();\n  const keywords = vero.vero_keywords || [];\n  \n  // ブランド名完全一致\n  if (brand === veroBrand) {\n    isBlocked = true;\n    matchedBrand = vero.brand_name;\n    riskLevel = vero.risk_level || 'high';\n    blockReason = 'ブランド名完全一致';\n    break;\n  }\n  \n  // タイトル内にブランド名含む\n  if (title.includes(veroBrand)) {\n    isBlocked = true;\n    matchedBrand = vero.brand_name;\n    riskLevel = vero.risk_level || 'medium';\n    blockReason = 'タイトル内にブランド名検出';\n    break;\n  }\n  \n  // キーワード検出\n  for (const kw of keywords) {\n    if (title.includes(kw.toLowerCase())) {\n      isBlocked = true;\n      matchedBrand = vero.brand_name;\n      riskLevel = vero.risk_level || 'medium';\n      blockReason = `キーワード「${kw}」検出`;\n      break;\n    }\n  }\n  if (isBlocked) break;\n}\n\nreturn [{ json: { user_id: request.user_id, product_id: request.product_id, is_blocked: isBlocked, matched_brand: matchedBrand, risk_level: riskLevel, block_reason: blockReason, image_urls: request.image_urls, needs_image_process: isBlocked && riskLevel !== 'critical' } }];"
      },
      "id": "check-vero",
      "name": "VEROブランド照合",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{ $json.needs_image_process }}",
              "value2": true
            }
          ]
        }
      },
      "id": "check-image-process",
      "name": "画像処理必要？",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "jsCode": "// V5-PRODUCTION: 内部トークン検証（crypto依存排除版）\n// ========================================\nconst body = $input.first().json.body || $input.first().json;\nconst headers = $input.first().json.headers || {};\n\n// 環境変数必須チェック\nconst apiUrl = $env.N3_API_URL;\nconst secret = $env.N8N_INTERNAL_SECRET;\nif (!apiUrl) {\n  return [{ json: { error: true, message: 'N3_API_URL 環境変数が未設定です', code: 'CONFIG_ERROR' } }];\n}\nif (!secret) {\n  return [{ json: { error: true, message: 'N8N_INTERNAL_SECRET 環境変数が未設定です', code: 'CONFIG_ERROR' } }];\n}\n\nconst internalToken = headers['x-n3-signature'] || headers['X-N3-Signature'];\nconst tokenTimestamp = headers['x-n3-token-timestamp'] || headers['X-N3-Token-Timestamp'];\nconst userId = body.user_id || 'default';\n\n// トークン検証（外部APIへ委譲）\nif (internalToken && tokenTimestamp) {\n  try {\n    const verifyResponse = await fetch(`${apiUrl}/api/n8n-auth/verify-internal-token`, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ userId, timestamp: tokenTimestamp, token: internalToken })\n    });\n    const verifyResult = await verifyResponse.json();\n    \n    if (!verifyResult.valid) {\n      return [{ json: { error: true, message: '内部トークンが無効です', code: 'INVALID_TOKEN' } }];\n    }\n  } catch (e) {\n    return [{ json: { error: true, message: 'トークン検証APIエラー: ' + e.message, code: 'VERIFY_API_ERROR' } }];\n  }\n}\n\n// 入力検証\nconst productId = body.product_id;\nconst marketplace = body.marketplace || 'ebay_us';\nconst limit = body.limit || 10;\nconst force = body.force || false;\n\nreturn [{ json: { \n  product_id: productId, \n  marketplace: marketplace, \n  user_id: userId,\n  limit: limit,\n  force: force,\n  request_timestamp: new Date().toISOString()\n} }];"
      },
      "id": "process-images",
      "name": "画像ハッシュ微修正",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "jsCode": "// 画像処理不要の場合はそのまま通過\nconst data = $input.first().json;\nreturn [{ json: { ...data, processed_images: null, image_processing_done: false } }];"
      },
      "id": "skip-images",
      "name": "画像処理スキップ",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "jsCode": "const data = $input.first().json;\nreturn [{ json: {\n  success: true,\n  user_id: data.user_id,\n  product_id: data.product_id,\n  vero_check: {\n    is_blocked: data.is_blocked,\n    matched_brand: data.matched_brand,\n    risk_level: data.risk_level,\n    block_reason: data.block_reason\n  },\n  image_processing: {\n    done: data.image_processing_done,\n    processed_images: data.processed_images\n  },\n  can_list: !data.is_blocked || data.risk_level !== 'critical',\n  ts: new Date().toISOString()\n} }];"
      },
      "id": "build-response",
      "name": "レスポンス構築",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO vero_check_logs (user_id, product_id, brand_checked, is_blocked, risk_level, image_processed, created_at) VALUES ($1, $2, $3, $4, $5, $6, NOW())",
        "options": {
          "queryParams": "={{ [{{ $json.user_id }}, {{ $json.product_id }}, {{ ($json.vero_check.matched_brand || '').replace(/'/g, \"''\") }}, {{ $json.vero_check.is_blocked }}, {{ $json.vero_check.risk_level }}, {{ $json.image_processing.done }}] }}"
        }
      },
      "id": "log-check",
      "name": "チェックログ記録",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      },
      "continueOnFail": true
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "DELETE FROM vero_check_logs WHERE created_at < NOW() - INTERVAL '30 days'",
        "options": {}
      },
      "id": "purge",
      "name": "🗑️ 履歴パージ（30日）",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      },
      "continueOnFail": true
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($node['レスポンス構築'].json) }}",
        "options": {
          "responseCode": 200
        }
      },
      "id": "respond",
      "name": "成功応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1
    },
    {
      "parameters": {
        "jsCode": "// 🔐 HMAC SHA256 署名検証 (Empire OS v6)\nconst crypto = require('crypto');\n\nconst signature = $request.headers['x-n3-signature'] || $request.headers['X-N3-Signature'];\nconst timestamp = $request.headers['x-n3-timestamp'] || $request.headers['X-N3-Timestamp'];\nconst secret = $env.N8N_HMAC_SECRET;\n\nif (!secret) {\n  throw new Error('🚫 N8N_HMAC_SECRET 環境変数が未設定です');\n}\n\nif (!signature || !timestamp) {\n  throw new Error('🚫 署名またはタイムスタンプが欠落しています');\n}\n\n// タイムスタンプ有効期限チェック (5分)\nconst now = Date.now();\nconst tsAge = now - parseInt(timestamp);\nif (tsAge > 300000 || tsAge < -30000) {\n  throw new Error('🚫 タイムスタンプが期限切れまたは不正です');\n}\n\n// HMAC検証\nconst bodyString = JSON.stringify($json);\nconst computedHmac = crypto\n  .createHmac('sha256', secret)\n  .update(timestamp + '.' + bodyString)\n  .digest('hex');\n\nif (signature !== computedHmac) {\n  throw new Error('🚫 HMAC署名検証失敗: 不正なリクエスト');\n}\n\n// 検証成功 - 元データをそのまま返す\nreturn [{ json: $json }];"
      },
      "name": "🔐 HMAC署名検証",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[EMPIRE_OS_V6] セキュリティ強制挿入"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{$env.SUPABASE_URL}}/rest/v1/execution_logs",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "apikey",
              "value": "={{$env.SUPABASE_ANON_KEY}}"
            },
            {
              "name": "Authorization",
              "value": "=Bearer {{$env.SUPABASE_SERVICE_ROLE_KEY}}"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Prefer",
              "value": "return=minimal"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\n  \"workflow_id\": \"{{$workflow.id}}\",\n  \"workflow_name\": \"{{$workflow.name}}\",\n  \"status\": \"success\",\n  \"execution_time_ms\": {{Date.now() - $execution.startedAt.getTime()}},\n  \"error_message\": null,\n  \"executed_at\": \"{{new Date().toISOString()}}\",\n  \"node_count\": {{$workflow.nodes.length}},\n  \"metadata\": {}\n}",
        "options": {
          "retry": {
            "attempts": 3,
            "delay": 1000,
            "backoff": "exponential"
          },
          "timeout": 30000
        }
      },
      "name": "📊 実行ログ送信 (成功)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true,
      "notes": "[EMPIRE_OS_V6] 中央監視パルス\n[PATCHER] WARNING: このノードはどこにも接続されていません。削除または接続してください。"
    }
  ],
  "connections": {
    "Webhook: 出品前チェック依頼": {
      "main": [
        [
          {
            "node": "🔐 HMAC署名検証",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "リクエスト検証": {
      "main": [
        [
          {
            "node": "エラーチェック",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "エラーチェック": {
      "main": [
        [
          {
            "node": "エラー応答",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "禁止ブランドリスト取得（user_id検証）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "禁止ブランドリスト取得（user_id検証）": {
      "main": [
        [
          {
            "node": "VEROブランド照合",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "VEROブランド照合": {
      "main": [
        [
          {
            "node": "画像処理必要？",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "画像処理必要？": {
      "main": [
        [
          {
            "node": "画像ハッシュ微修正",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "画像処理スキップ",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "画像ハッシュ微修正": {
      "main": [
        [
          {
            "node": "レスポンス構築",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "画像処理スキップ": {
      "main": [
        [
          {
            "node": "レスポンス構築",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "レスポンス構築": {
      "main": [
        [
          {
            "node": "チェックログ記録",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "チェックログ記録": {
      "main": [
        [
          {
            "node": "🗑️ 履歴パージ（30日）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🗑️ 履歴パージ（30日）": {
      "main": [
        [
          {
            "node": "成功応答",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🔐 HMAC署名検証": {
      "main": [
        [
          {
            "node": "リクエスト検証",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionTimeout": 120,
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "none"
  },
  "tags": [
    {
      "name": "N3-VERO",
      "color": "#dc143c"
    },
    {
      "name": "V4-PRODUCTION",
      "color": "#ff0000"
    },
    {
      "name": "Multi-Tenant",
      "color": "#00bfff"
    },
    {
      "name": "Internal-Auth",
      "color": "#dc143c"
    },
    {
      "name": "500MB-Safe",
      "color": "#32cd32"
    },
    {
      "name": "Empire-OS-V6",
      "color": "#ff4500"
    }
  ],
  "active": false
}