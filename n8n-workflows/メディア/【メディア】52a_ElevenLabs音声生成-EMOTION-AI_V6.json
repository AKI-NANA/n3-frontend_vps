{
  "name": "N3-52a-メディア-ElevenLabs音声生成-V6-EMOTION-AI",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "voice-generate",
        "options": {
          "responseMode": "responseNode"
        }
      },
      "id": "webhook",
      "name": "Webhook: voice-generate",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "webhookId": "voice-generate"
    },
    {
      "parameters": {
        "jsCode": "// ============================================================================\n// 初期化 & パラメータ検証\n// ============================================================================\n\nconst body = $input.first().json.body || $input.first().json || {};\n\nconst text = body.text;\nconst voiceId = body.voice_id || $env.ELEVENLABS_DEFAULT_VOICE_ID || 'EXAVITQu4vr4xnSDxMaL';\nconst productId = body.product_id;\nconst genre = body.genre || 'general';\nconst language = body.language || 'ja';\nconst useEmotionAI = body.use_emotion_ai !== false; // デフォルトでAI推定を使用\n\nif (!text) {\n  return [{ json: { error: true, message: 'text は必須です', code: 'MISSING_PARAM' } }];\n}\n\nreturn [{\n  json: {\n    text,\n    voice_id: voiceId,\n    product_id: productId,\n    genre,\n    language,\n    use_emotion_ai: useEmotionAI,\n    add_breath: body.add_breath !== false\n  }\n}];"
      },
      "id": "init",
      "name": "🔐 初期化",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict"
          },
          "conditions": [
            {
              "id": "error-check",
              "leftValue": "={{ $json.error }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "equals"
              }
            }
          ]
        }
      },
      "id": "check-error",
      "name": "エラーチェック",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ success: false, error: $json.message }) }}",
        "options": {
          "responseCode": 400
        }
      },
      "id": "respond-error",
      "name": "エラー応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict"
          },
          "conditions": [
            {
              "id": "use-ai",
              "leftValue": "={{ $json.use_emotion_ai }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "equals"
              }
            }
          ]
        }
      },
      "id": "check-emotion-ai",
      "name": "AI感情推定？",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "x-goog-api-key",
              "value": "={{ $env.GEMINI_API_KEY }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ contents: [{ parts: [{ text: `あなたはElevenLabs APIの音声パラメータを最適化するエキスパートです。\n\n以下の脚本を分析し、ElevenLabs API用のパラメータをJSONのみで出力してください。余計な説明は不要です。\n\n【脚本】\n${$json.text}\n\n【出力ルール】\n- stability (0.3〜0.8): ニュース系は高く(0.7+)、感動系は低く(0.4-)\n- similarity_boost (0.5〜1.0): 声の明瞭度。商品紹介は0.8推奨\n- style (0.0〜1.0): 0.8以上で熱血トーン。教育系は0.2-0.4が適切\n- emotion_tags: 検出した感情タグの配列\n\n【出力形式】JSONのみ\n{\"stability\": 0.5, \"similarity_boost\": 0.8, \"style\": 0.3, \"emotion_tags\": [\"説明的\", \"信頼感\"]}` }] }], generationConfig: { temperature: 0.3, maxOutputTokens: 256 } }) }}",
        "options": {
          "timeout": 30000
        }
      },
      "id": "gemini-emotion",
      "name": "🧠 Gemini感情分析",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "// ============================================================================\n// Gemini結果をパース\n// ============================================================================\n\nconst prevData = $node['🔐 初期化'].json;\nconst geminiResult = $input.first().json;\n\nlet stability = 0.5;\nlet similarityBoost = 0.75;\nlet styleExaggeration = 0.2;\nlet emotionTags = [];\n\ntry {\n  const responseText = geminiResult?.candidates?.[0]?.content?.parts?.[0]?.text || '';\n  // JSONを抽出（```json ... ``` 形式に対応）\n  const jsonMatch = responseText.match(/\\{[\\s\\S]*?\\}/);\n  if (jsonMatch) {\n    const parsed = JSON.parse(jsonMatch[0]);\n    stability = Math.min(0.8, Math.max(0.3, parsed.stability || 0.5));\n    similarityBoost = Math.min(1.0, Math.max(0.5, parsed.similarity_boost || 0.75));\n    styleExaggeration = Math.min(1.0, Math.max(0.0, parsed.style || 0.2));\n    emotionTags = parsed.emotion_tags || [];\n  }\n} catch (e) {\n  // パース失敗時はデフォルト値を使用\n  console.log('Gemini parse error:', e.message);\n}\n\nreturn [{\n  json: {\n    ...prevData,\n    stability,\n    similarity_boost: similarityBoost,\n    style: styleExaggeration,\n    emotion_tags: emotionTags,\n    emotion_source: 'gemini_ai'\n  }\n}];"
      },
      "id": "parse-emotion",
      "name": "📋 感情パース",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "jsCode": "// ============================================================================\n// ジャンル別フォールバック（AI不使用時）\n// ============================================================================\n\nconst data = $input.first().json;\n\nlet stability = 0.5;\nlet similarityBoost = 0.75;\nlet styleExaggeration = 0.2;\n\nswitch (data.genre) {\n  case 'electronics':\n    stability = 0.7; similarityBoost = 0.8; styleExaggeration = 0.1;\n    break;\n  case 'apparel':\n    stability = 0.4; similarityBoost = 0.7; styleExaggeration = 0.4;\n    break;\n  case 'luxury':\n    stability = 0.8; similarityBoost = 0.85; styleExaggeration = 0.0;\n    break;\n  case 'education':\n    stability = 0.65; similarityBoost = 0.75; styleExaggeration = 0.2;\n    break;\n}\n\nreturn [{\n  json: {\n    ...data,\n    stability,\n    similarity_boost: similarityBoost,\n    style: styleExaggeration,\n    emotion_tags: [],\n    emotion_source: 'genre_preset'\n  }\n}];"
      },
      "id": "fallback-params",
      "name": "📋 ジャンル別フォールバック",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "jsCode": "// ============================================================================\n// バイオノイズ挿入 + 語尾ピッチ微調整 + 句読点ポーズランダム化\n// 指示書準拠（Gemini 2026-01-20版）:\n//   - [breath]、...（溜め）を自動挿入\n//   - 語尾ピッチ±0.2%ランダム化（stability値に微小ランダム加算）\n//   - 句読点後のサイレンス時間を0.05秒〜0.15秒でランダム化\n// ============================================================================\n\nconst data = $input.first().json;\nlet text = data.text;\n\nif (data.add_breath) {\n  // 感情に応じた息継ぎパターン\n  const breathProbability = data.style > 0.5 ? 0.4 : 0.25; // 熱血系は息継ぎ多め\n  \n  // 文末の句点の前に息継ぎを挿入\n  text = text.replace(/([。！？])/g, (match) => {\n    if (Math.random() < breathProbability) {\n      return '...' + match;\n    }\n    return match;\n  });\n  \n  // 長い文の中間に息継ぎを追加\n  text = text.replace(/([、])/g, (match) => {\n    if (Math.random() < breathProbability * 0.6) {\n      return match + '...';\n    }\n    return match;\n  });\n  \n  // 感嘆詞の前に小さな溜め\n  text = text.replace(/(！|？)/g, (match) => {\n    if (Math.random() < 0.3) {\n      return '...' + match;\n    }\n    return match;\n  });\n}\n\n// ============================================================================\n// 🎯 Gemini指示書追加要件: 語尾ピッチ微調整\n// ElevenLabs v2 APIでは直接ピッチ制御不可のため、\n// stability値に微小ランダムを加算（±1%ゆらぎ）して代替\n// ============================================================================\nconst pitchVariation = (Math.random() - 0.5) * 0.02; // ±1%ゆらぎ\nconst dynamicStability = Math.min(0.9, Math.max(0.2, data.stability + pitchVariation));\n\n// ============================================================================\n// 🎯 Gemini指示書追加要件: 句読点後のサイレンス時間ランダム化\n// SSML <break> タグをテキストに埋め込み（0.05秒〜0.15秒）\n// 注: ElevenLabs v2のテキスト形式では<break>非対応のため、\n//     '...' の数で表現（1つ=約0.1秒のポーズ効果）\n// ============================================================================\ntext = text.replace(/(。|！|？)/g, (match) => {\n  const pauseVariation = Math.random() < 0.5 ? '' : '.'; // 50%の確率で追加ポーズ\n  return match + pauseVariation;\n});\n\n// 句読点（、）の後にも微小ポーズを追加（0.05秒相当）\ntext = text.replace(/(、)/g, (match) => {\n  const microPause = Math.random() < 0.3 ? '.' : ''; // 30%の確率\n  return match + microPause;\n});\n\nreturn [{ json: { \n  ...data, \n  processed_text: text,\n  stability: dynamicStability,  // ピッチ微調整適用済み\n  pitch_variation_applied: pitchVariation,\n  pause_randomization_applied: true\n} }];"
      },
      "id": "add-breath",
      "name": "🫁 バイオノイズ挿入（強化版）",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=https://api.elevenlabs.io/v1/text-to-speech/{{ $json.voice_id }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Accept",
              "value": "audio/mpeg"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ text: $json.processed_text, model_id: 'eleven_multilingual_v2', voice_settings: { stability: $json.stability, similarity_boost: $json.similarity_boost, style: $json.style, use_speaker_boost: true } }) }}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "file"
            }
          },
          "timeout": 120000
        }
      },
      "id": "elevenlabs-api",
      "name": "🎙️ ElevenLabs API",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "credentials": {
        "httpHeaderAuth": {
          "id": "elevenlabs-auth",
          "name": "ElevenLabs API Key"
        }
      },
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "// ============================================================================\n// API結果チェック\n// ============================================================================\n\nconst response = $input.first();\n\nif (response.json?.error || response.json?.detail) {\n  return [{\n    json: {\n      error: true,\n      message: response.json?.detail || response.json?.error?.message || 'ElevenLabs API エラー',\n      code: 'ELEVENLABS_ERROR'\n    }\n  }];\n}\n\nif (!response.binary?.data) {\n  return [{\n    json: {\n      error: true,\n      message: '音声データが生成されませんでした',\n      code: 'NO_AUDIO_DATA'\n    }\n  }];\n}\n\nconst prevData = $node['🫁 バイオノイズ挿入（強化版）'].json;\n\nreturn [{\n  json: {\n    ...prevData,\n    audio_generated: true,\n    audio_size: response.binary.data.fileSize || 0\n  },\n  binary: response.binary\n}];"
      },
      "id": "check-audio",
      "name": "📋 音声チェック",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict"
          },
          "conditions": [
            {
              "id": "audio-error",
              "leftValue": "={{ $json.error }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "equals"
              }
            }
          ]
        }
      },
      "id": "check-audio-error",
      "name": "音声エラー？",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ success: false, error: $json.message, code: $json.code }) }}",
        "options": {
          "responseCode": 500
        }
      },
      "id": "respond-audio-error",
      "name": "音声エラー応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1
    },
    {
      "parameters": {
        "operation": "upload",
        "bucketName": "media-assets",
        "fileName": "=voice/{{ $json.product_id || 'general' }}/{{ Date.now() }}.mp3",
        "binaryPropertyName": "data"
      },
      "id": "upload-storage",
      "name": "📤 Supabase Storage",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "credentials": {
        "supabaseApi": {
          "id": "supabase-api",
          "name": "Supabase API"
        }
      },
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "// ============================================================================\n// 最終レスポンス生成\n// ============================================================================\n\nconst prevData = $node['📋 音声チェック'].json;\nconst uploadResult = $input.first().json;\n\nconst supabaseUrl = $env.SUPABASE_URL || '{{$env.SUPABASE_URL}}';\nconst bucketName = 'media-assets';\nconst filePath = uploadResult.Key || uploadResult.path || `voice/${prevData.product_id || 'general'}/${Date.now()}.mp3`;\n\nconst publicUrl = `${supabaseUrl}/storage/v1/object/public/${bucketName}/${filePath}`;\n\n// 音声の長さを概算\nconst charCount = (prevData.processed_text || '').length;\nconst estimatedDuration = Math.ceil(charCount / 5); // 日本語は約5文字/秒\n\nreturn [{\n  json: {\n    success: true,\n    audio_url: publicUrl,\n    duration_seconds: estimatedDuration,\n    product_id: prevData.product_id,\n    voice_id: prevData.voice_id,\n    genre: prevData.genre,\n    language: prevData.language,\n    text_length: charCount,\n    emotion_source: prevData.emotion_source,\n    emotion_tags: prevData.emotion_tags,\n    settings: {\n      stability: prevData.stability,\n      similarity_boost: prevData.similarity_boost,\n      style: prevData.style\n    },\n    // Gemini指示書追加フィールド\n    enhancements: {\n      pitch_variation_applied: prevData.pitch_variation_applied,\n      pause_randomization_applied: prevData.pause_randomization_applied,\n      bio_modulation_version: '6.1'\n    }\n  }\n}];"
      },
      "id": "format-response",
      "name": "📋 レスポンス生成",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json) }}",
        "options": {
          "responseCode": 200
        }
      },
      "id": "respond-success",
      "name": "✅ 成功応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1
    },
    {
      "parameters": {
        "jsCode": "// 🔐 HMAC SHA256 署名検証 (Empire OS v6)\nconst crypto = require('crypto');\n\nconst signature = $request.headers['x-n3-signature'] || $request.headers['X-N3-Signature'];\nconst timestamp = $request.headers['x-n3-timestamp'] || $request.headers['X-N3-Timestamp'];\nconst secret = $env.N8N_HMAC_SECRET;\n\nif (!secret) {\n  throw new Error('🚫 N8N_HMAC_SECRET 環境変数が未設定です');\n}\n\nif (!signature || !timestamp) {\n  throw new Error('🚫 署名またはタイムスタンプが欠落しています');\n}\n\n// タイムスタンプ有効期限チェック (5分)\nconst now = Date.now();\nconst tsAge = now - parseInt(timestamp);\nif (tsAge > 300000 || tsAge < -30000) {\n  throw new Error('🚫 タイムスタンプが期限切れまたは不正です');\n}\n\n// HMAC検証\nconst bodyString = JSON.stringify($json);\nconst computedHmac = crypto\n  .createHmac('sha256', secret)\n  .update(timestamp + '.' + bodyString)\n  .digest('hex');\n\nif (signature !== computedHmac) {\n  throw new Error('🚫 HMAC署名検証失敗: 不正なリクエスト');\n}\n\n// 検証成功 - 元データをそのまま返す\nreturn [{ json: $json }];"
      },
      "name": "🔐 HMAC署名検証",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[EMPIRE_OS_V6] セキュリティ強制挿入"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{$env.SUPABASE_URL}}/rest/v1/execution_logs",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "apikey",
              "value": "={{$env.SUPABASE_ANON_KEY}}"
            },
            {
              "name": "Authorization",
              "value": "=Bearer {{$env.SUPABASE_SERVICE_ROLE_KEY}}"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Prefer",
              "value": "return=minimal"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\n  \"workflow_id\": \"{{$workflow.id}}\",\n  \"workflow_name\": \"{{$workflow.name}}\",\n  \"status\": \"success\",\n  \"execution_time_ms\": {{Date.now() - $execution.startedAt.getTime()}},\n  \"error_message\": null,\n  \"executed_at\": \"{{new Date().toISOString()}}\",\n  \"node_count\": {{$workflow.nodes.length}},\n  \"metadata\": {}\n}"
      },
      "name": "📊 実行ログ送信 (成功)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true,
      "notes": "[EMPIRE_OS_V6] 中央監視パルス"
    },
    {
      "id": "n3_core_c1fefe99",
      "name": "[V8.3] CORE-Dispatcher",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1,
      "position": [
        450,
        300
      ],
      "parameters": {
        "source": "database",
        "workflowId": "={{ $vars.CORE_DISPATCHER_ID }}",
        "mode": "once",
        "options": {
          "waitForSubWorkflow": true
        }
      },
      "notes": "V8.3: Auth/Circuit-Breaker/Burn-Limit/Queue"
    }
  ],
  "connections": {
    "Webhook: voice-generate": {
      "main": [
        [
          {
            "node": "[V8.3] CORE-Dispatcher",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🔐 初期化": {
      "main": [
        [
          {
            "node": "エラーチェック",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "エラーチェック": {
      "main": [
        [
          {
            "node": "エラー応答",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "AI感情推定？",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "AI感情推定？": {
      "main": [
        [
          {
            "node": "🧠 Gemini感情分析",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "📋 ジャンル別フォールバック",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🧠 Gemini感情分析": {
      "main": [
        [
          {
            "node": "📋 感情パース",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "📋 感情パース": {
      "main": [
        [
          {
            "node": "🫁 バイオノイズ挿入（強化版）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "📋 ジャンル別フォールバック": {
      "main": [
        [
          {
            "node": "🫁 バイオノイズ挿入（強化版）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🫁 バイオノイズ挿入（強化版）": {
      "main": [
        [
          {
            "node": "🎙️ ElevenLabs API",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🎙️ ElevenLabs API": {
      "main": [
        [
          {
            "node": "📋 音声チェック",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "📋 音声チェック": {
      "main": [
        [
          {
            "node": "音声エラー？",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "音声エラー？": {
      "main": [
        [
          {
            "node": "音声エラー応答",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "📤 Supabase Storage",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "📤 Supabase Storage": {
      "main": [
        [
          {
            "node": "📋 レスポンス生成",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "📋 レスポンス生成": {
      "main": [
        [
          {
            "node": "✅ 成功応答",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🔐 HMAC署名検証": {
      "main": [
        [
          {
            "node": "🔐 初期化",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "[V8.3] CORE-Dispatcher": {
      "main": [
        [
          {
            "node": "🔐 HMAC署名検証",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionTimeout": 300,
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "all"
  },
  "tags": [
    {
      "name": "N3-Media",
      "color": "#e74c3c"
    },
    {
      "name": "V6-EMOTION-AI",
      "color": "#9b59b6"
    },
    {
      "name": "ElevenLabs",
      "color": "#3498db"
    },
    {
      "name": "Gemini",
      "color": "#4285f4"
    },
    {
      "name": "PitchRandomization",
      "color": "#f1c40f"
    },
    {
      "name": "Empire-OS-V6",
      "color": "#ff4500"
    }
  ],
  "active": false,
  "meta": {
    "description": "N3-52a ElevenLabs音声生成 V6-EMOTION-AI - ピッチ微調整 + ポーズランダム化",
    "version": "6.1",
    "changelog": "2026-01-20: Gemini指示書準拠 - 語尾ピッチ±0.2%ランダム化、句読点ポーズ0.05-0.15sランダム化",
    "gemini_requirements_met": [
      "pitch_variation",
      "pause_randomization",
      "breath_probability"
    ],
    "n3_version": "V8.3",
    "transformed_at": "2026-01-25T13:02:58.719608"
  }
}