{
  "name": "【メディア】14_50-メディアブリッジ-media-bridge_V5",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "media-bridge",
        "options": {
          "responseMode": "responseNode"
        }
      },
      "id": "webhook",
      "name": "Webhook: media-bridge",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "webhookId": "media-bridge"
    },
    {
      "parameters": {
        "jsCode": "// V5-PRODUCTION: 内部トークン検証（crypto依存排除版）\n// ========================================\nconst body = $input.first().json.body || $input.first().json;\nconst headers = $input.first().json.headers || {};\n\n// 環境変数必須チェック\nconst apiUrl = $env.N3_API_URL;\nconst secret = $env.N8N_INTERNAL_SECRET;\nif (!apiUrl) {\n  return [{ json: { error: true, message: 'N3_API_URL 環境変数が未設定です', code: 'CONFIG_ERROR' } }];\n}\nif (!secret) {\n  return [{ json: { error: true, message: 'N8N_INTERNAL_SECRET 環境変数が未設定です', code: 'CONFIG_ERROR' } }];\n}\n\nconst internalToken = headers['x-n3-signature'] || headers['X-N3-Signature'];\nconst tokenTimestamp = headers['x-n3-token-timestamp'] || headers['X-N3-Token-Timestamp'];\nconst userId = body.user_id || 'default';\n\n// トークン検証（外部APIへ委譲）\nif (internalToken && tokenTimestamp) {\n  try {\n    const verifyResponse = await fetch(`${apiUrl}/api/n8n-auth/verify-internal-token`, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ userId, timestamp: tokenTimestamp, token: internalToken })\n    });\n    const verifyResult = await verifyResponse.json();\n    \n    if (!verifyResult.valid) {\n      return [{ json: { error: true, message: '内部トークンが無効です', code: 'INVALID_TOKEN' } }];\n    }\n  } catch (e) {\n    return [{ json: { error: true, message: 'トークン検証APIエラー: ' + e.message, code: 'VERIFY_API_ERROR' } }];\n  }\n}\n\n// 入力検証\nconst productId = body.product_id;\nconst marketplace = body.marketplace || 'ebay_us';\nconst limit = body.limit || 10;\nconst force = body.force || false;\n\nreturn [{ json: { \n  product_id: productId, \n  marketplace: marketplace, \n  user_id: userId,\n  limit: limit,\n  force: force,\n  request_timestamp: new Date().toISOString()\n} }];"
      },
      "id": "validate",
      "name": "🔐 内部トークン検証 + 入力検証",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict"
          },
          "conditions": [
            {
              "id": "error-check",
              "leftValue": "={{ $json.error }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "equals"
              }
            }
          ]
        }
      },
      "id": "check-error",
      "name": "エラーチェック",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ success: false, error: $json.message, code: $json.code }) }}",
        "options": {
          "responseCode": "={{ $json.code === 'INVALID_TOKEN' ? 403 : 400 }}"
        }
      },
      "id": "respond-error",
      "name": "エラー応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT pm.id as product_id, pm.sku, pm.product_name, pm.title_en, pm.listing_price, pm.image_urls, pm.currency, ROUND((pm.listing_price - COALESCE(im.purchase_price, 0)) / NULLIF(COALESCE(im.purchase_price, 1), 0) * 100, 2) as profit_rate, ml.marketplace_id as marketplace FROM products_master pm LEFT JOIN inventory_master im ON im.product_master_id = pm.id AND im.user_id = pm.user_id LEFT JOIN marketplace_listings ml ON ml.product_master_id = pm.id AND ml.user_id = pm.user_id WHERE pm.id = ANY(ARRAY[$1]::integer[]) AND pm.user_id = $2 AND ROUND((pm.listing_price - COALESCE(im.purchase_price, 0)) / NULLIF(COALESCE(im.purchase_price, 1), 0) * 100, 2) >= $3",
        "options": {
          "queryParams": "={{ [{{ $json.product_ids.join(',') }}, {{ $json.user_id }}, {{ $json.min_profit_rate }}] }}"
        }
      },
      "id": "get-products",
      "name": "📦 対象商品取得（利益率フィルタ）",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// ========================================\n// V4-PRODUCTION: media_queue 登録データ準備\n// 新スキーマ対応（product_id単一、benefit_points等）\n// ========================================\nconst products = $input.all().map(item => item.json);\nconst request = $node['🔐 内部トークン検証 + 入力検証'].json;\n\n// 結果がない場合（配列が空 or 最初の要素がnull）\nconst validProducts = products.filter(p => p && p.product_id);\n\nif (validProducts.length === 0) {\n  return [{ json: { \n    error: false, \n    message: '利益率条件を満たす商品がありません', \n    registered_count: 0,\n    products: []\n  } }];\n}\n\n// 各商品をmedia_queue用に整形\nconst mediaItems = validProducts.map(p => ({\n  product_id: p.product_id,\n  user_id: request.user_id,\n  image_urls: p.image_urls || [],\n  title_en: (p.title_en || p.product_name || '').substring(0, 500),\n  listing_price: p.listing_price || 0,\n  currency: p.currency || 'USD',\n  marketplace: p.marketplace || 'ebay_us',\n  profit_rate: p.profit_rate || 0,\n  status: 'pending',\n  benefit_points: [] // Gemini処理待ち\n}));\n\nreturn [{ json: {\n  user_id: request.user_id,\n  products: mediaItems,\n  count: mediaItems.length\n} }];"
      },
      "id": "prepare-media",
      "name": "📝 media_queue登録データ準備",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict"
          },
          "conditions": [
            {
              "id": "has-products",
              "leftValue": "={{ $json.count }}",
              "rightValue": 0,
              "operator": {
                "type": "number",
                "operation": "gt"
              }
            }
          ]
        }
      },
      "id": "check-products",
      "name": "登録対象あり？",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ success: true, message: $json.message, registered_count: 0 }) }}",
        "options": {
          "responseCode": 200
        }
      },
      "id": "respond-no-products",
      "name": "対象なし応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1
    },
    {
      "parameters": {
        "jsCode": "// 各商品をループ用に展開\nconst data = $input.first().json;\nreturn data.products.map(p => ({ json: p }));"
      },
      "id": "split-products",
      "name": "商品分割",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO media_queue (product_id, user_id, image_urls, title_en, listing_price, currency, marketplace, profit_rate, status, created_at, updated_at) VALUES ($1, $2, $3::jsonb, $4, $5, $6, $7, $8, 'pending', NOW(), NOW()) ON CONFLICT (product_id, user_id) DO UPDATE SET image_urls = EXCLUDED.image_urls, title_en = EXCLUDED.title_en, listing_price = EXCLUDED.listing_price, profit_rate = EXCLUDED.profit_rate, status = CASE WHEN media_queue.status IN ('completed', 'failed') THEN 'pending' ELSE media_queue.status END, updated_at = NOW() RETURNING id, product_id, status",
        "options": {
          "queryParams": "={{ [{{ $json.product_id }}, {{ $json.user_id }}, {{ JSON.stringify($json.image_urls) }}, {{ $json.title_en.replace(/'/g, \"''\") }}, {{ $json.listing_price }}, {{ $json.currency }}, {{ $json.marketplace }}, {{ $json.profit_rate }}] }}"
        }
      },
      "id": "insert-media-queue",
      "name": "📥 media_queue UPSERT",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      },
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "// 登録結果を集約\nconst results = $input.all().map(item => item.json);\nconst validResults = results.filter(r => r && r.id);\nconst prepareData = $node['📝 media_queue登録データ準備'].json;\n\nreturn [{ json: {\n  success: true,\n  registered_count: validResults.length,\n  requested_count: prepareData.count,\n  user_id: prepareData.user_id,\n  registered_ids: validResults.map(r => r.id),\n  message: `${validResults.length}件をメディアキューに登録しました`\n} }];"
      },
      "id": "collect-results",
      "name": "📊 結果集約",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json) }}",
        "options": {
          "responseCode": 200
        }
      },
      "id": "respond-success",
      "name": "✅ 成功応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "DELETE FROM media_queue WHERE status IN ('completed', 'failed') AND created_at < NOW() - INTERVAL '7 days' AND user_id = $1;",
        "options": {
          "queryParams": "={{ [{{ $node['📝 media_queue登録データ準備'].json.user_id }}] }}"
        }
      },
      "id": "purge-history",
      "name": "🗑️ media_queueパージ（7日超完了/失敗）",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      },
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "// 🔐 HMAC SHA256 署名検証 (Empire OS v6)\nconst crypto = require('crypto');\n\nconst signature = $request.headers['x-n3-signature'] || $request.headers['X-N3-Signature'];\nconst timestamp = $request.headers['x-n3-timestamp'] || $request.headers['X-N3-Timestamp'];\nconst secret = $env.N8N_HMAC_SECRET;\n\nif (!secret) {\n  throw new Error('🚫 N8N_HMAC_SECRET 環境変数が未設定です');\n}\n\nif (!signature || !timestamp) {\n  throw new Error('🚫 署名またはタイムスタンプが欠落しています');\n}\n\n// タイムスタンプ有効期限チェック (5分)\nconst now = Date.now();\nconst tsAge = now - parseInt(timestamp);\nif (tsAge > 300000 || tsAge < -30000) {\n  throw new Error('🚫 タイムスタンプが期限切れまたは不正です');\n}\n\n// HMAC検証\nconst bodyString = JSON.stringify($json);\nconst computedHmac = crypto\n  .createHmac('sha256', secret)\n  .update(timestamp + '.' + bodyString)\n  .digest('hex');\n\nif (signature !== computedHmac) {\n  throw new Error('🚫 HMAC署名検証失敗: 不正なリクエスト');\n}\n\n// 検証成功 - 元データをそのまま返す\nreturn [{ json: $json }];"
      },
      "name": "🔐 HMAC署名検証",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[EMPIRE_OS_V6] セキュリティ強制挿入"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{$env.SUPABASE_URL}}/rest/v1/execution_logs",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "apikey",
              "value": "={{$env.SUPABASE_ANON_KEY}}"
            },
            {
              "name": "Authorization",
              "value": "=Bearer {{$env.SUPABASE_SERVICE_ROLE_KEY}}"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Prefer",
              "value": "return=minimal"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\n  \"workflow_id\": \"{{$workflow.id}}\",\n  \"workflow_name\": \"{{$workflow.name}}\",\n  \"status\": \"success\",\n  \"execution_time_ms\": {{Date.now() - $execution.startedAt.getTime()}},\n  \"error_message\": null,\n  \"executed_at\": \"{{new Date().toISOString()}}\",\n  \"node_count\": {{$workflow.nodes.length}},\n  \"metadata\": {}\n}",
        "options": {
          "retry": {
            "attempts": 3,
            "delay": 1000,
            "backoff": "exponential"
          },
          "timeout": 30000
        }
      },
      "name": "📊 実行ログ送信 (成功)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true,
      "notes": "[EMPIRE_OS_V6] 中央監視パルス"
    }
  ],
  "connections": {
    "Webhook: media-bridge": {
      "main": [
        [
          {
            "node": "🔐 HMAC署名検証",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🔐 内部トークン検証 + 入力検証": {
      "main": [
        [
          {
            "node": "エラーチェック",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "エラーチェック": {
      "main": [
        [
          {
            "node": "エラー応答",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "📦 対象商品取得（利益率フィルタ）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "📦 対象商品取得（利益率フィルタ）": {
      "main": [
        [
          {
            "node": "📝 media_queue登録データ準備",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "📝 media_queue登録データ準備": {
      "main": [
        [
          {
            "node": "登録対象あり？",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "登録対象あり？": {
      "main": [
        [
          {
            "node": "商品分割",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "対象なし応答",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "商品分割": {
      "main": [
        [
          {
            "node": "📥 media_queue UPSERT",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "📥 media_queue UPSERT": {
      "main": [
        [
          {
            "node": "📊 結果集約",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "📊 結果集約": {
      "main": [
        [
          {
            "node": "✅ 成功応答",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "✅ 成功応答": {
      "main": [
        [
          {
            "node": "🗑️ media_queueパージ（7日超完了/失敗）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🔐 HMAC署名検証": {
      "main": [
        [
          {
            "node": "🔐 内部トークン検証 + 入力検証",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🗑️ media_queueパージ（7日超完了/失敗）": {
      "main": [
        [
          {
            "node": "📊 実行ログ送信 (成功)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionTimeout": 120,
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "all"
  },
  "tags": [
    {
      "name": "N3-Media",
      "color": "#9b59b6"
    },
    {
      "name": "V4-PRODUCTION",
      "color": "#ff0000"
    },
    {
      "name": "帝国OS",
      "color": "#e74c3c"
    },
    {
      "name": "Multi-Tenant",
      "color": "#00bfff"
    },
    {
      "name": "Internal-Auth",
      "color": "#dc143c"
    },
    {
      "name": "History-Purge",
      "color": "#ffa500"
    },
    {
      "name": "Empire-OS-V6",
      "color": "#ff4500"
    }
  ],
  "active": false
}