{
  "name": "【司令塔】06_16-仕入管理-自動仕入先切替_V5",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "supplier-switch",
        "options": {
          "responseMode": "responseNode"
        }
      },
      "id": "webhook",
      "name": "Webhook: 仕入先切替",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "webhookId": "supplier-switch"
    },
    {
      "parameters": {
        "jsCode": "// V5-PRODUCTION: 内部トークン検証（crypto依存排除版）\n// ========================================\nconst body = $input.first().json.body || $input.first().json;\nconst headers = $input.first().json.headers || {};\n\n// 環境変数必須チェック\nconst apiUrl = $env.N3_API_URL;\nconst secret = $env.N8N_INTERNAL_SECRET;\nif (!apiUrl) {\n  return [{ json: { error: true, message: 'N3_API_URL 環境変数が未設定です', code: 'CONFIG_ERROR' } }];\n}\nif (!secret) {\n  return [{ json: { error: true, message: 'N8N_INTERNAL_SECRET 環境変数が未設定です', code: 'CONFIG_ERROR' } }];\n}\n\nconst internalToken = headers['x-n3-signature'] || headers['X-N3-Signature'];\nconst tokenTimestamp = headers['x-n3-token-timestamp'] || headers['X-N3-Token-Timestamp'];\nconst userId = body.user_id || 'default';\n\n// トークン検証（外部APIへ委譲）\nif (internalToken && tokenTimestamp) {\n  try {\n    const verifyResponse = await fetch(`${apiUrl}/api/n8n-auth/verify-internal-token`, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ userId, timestamp: tokenTimestamp, token: internalToken })\n    });\n    const verifyResult = await verifyResponse.json();\n    \n    if (!verifyResult.valid) {\n      return [{ json: { error: true, message: '内部トークンが無効です', code: 'INVALID_TOKEN' } }];\n    }\n  } catch (e) {\n    return [{ json: { error: true, message: 'トークン検証APIエラー: ' + e.message, code: 'VERIFY_API_ERROR' } }];\n  }\n}\n\n// 入力検証\nconst productId = body.product_id;\nconst marketplace = body.marketplace || 'ebay_us';\nconst limit = body.limit || 10;\nconst force = body.force || false;\n\nreturn [{ json: { \n  product_id: productId, \n  marketplace: marketplace, \n  user_id: userId,\n  limit: limit,\n  force: force,\n  request_timestamp: new Date().toISOString()\n} }];"
      },
      "id": "validate",
      "name": "リクエスト検証",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{ $json.error }}",
              "value2": true
            }
          ]
        }
      },
      "id": "check-error",
      "name": "エラーチェック",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ success: false, error: $json.message }) }}",
        "options": {
          "responseCode": 403
        }
      },
      "id": "respond-error",
      "name": "エラー応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT pm.id, pm.sku, pm.product_name, pm.title, pm.jan_code, pm.model_number, pm.source_url, pm.source_price_jpy, pm.user_id, pm.updated_at FROM products_master pm WHERE pm.id = $1 AND pm.user_id = $2",
        "options": {
          "queryParams": "={{ [{{ $json.product_id }}, {{ $json.user_id }}] }}"
        }
      },
      "id": "get-product",
      "name": "商品情報取得（user_id検証）",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// 仕入先候補リスト生成\nconst products = Array.isArray($input.first().json) ? $input.first().json : [$input.first().json];\nconst product = products[0] || {};\nconst request = $node['リクエスト検証'].json;\n\nif (!product.id) return [{ json: { error: true, message: '商品が見つかりません' } }];\n\nconst title = product.product_name || product.title || '';\nconst keywords = title.split(/[\\s\\-\\_\\/【】\\[\\]（）]+/).filter(w => w.length > 2);\n\nconst searchTerms = [];\nif (product.jan_code) searchTerms.push({ type: 'jan', query: product.jan_code });\nif (product.model_number) searchTerms.push({ type: 'model', query: product.model_number });\nsearchTerms.push({ type: 'keyword', query: keywords.slice(0, 5).join(' ') });\n\nconst suppliers = [\n  { name: 'yahoo_auction', priority: 1 },\n  { name: 'mercari', priority: 2 },\n  { name: 'rakuma', priority: 3 },\n  { name: 'amazon_jp', priority: 4 }\n];\n\nreturn [{ json: {\n  user_id: request.user_id,\n  product_id: product.id,\n  updated_at: product.updated_at,\n  product_name: title,\n  original_source: product.source_url,\n  original_price: product.source_price_jpy || 0,\n  search_terms: searchTerms,\n  suppliers: suppliers,\n  max_price: (product.source_price_jpy || 10000) * 1.2\n} }];"
      },
      "id": "prepare-search",
      "name": "検索準備",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.N3_API_URL || '{{$env.GATEWAY_URL}}' }}/api/ai/analyze",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ model: 'gemini-1.5-flash', prompt: '以下の商品の代替仕入先を探すための検索クエリを生成してください。\\n\\n商品名: ' + $json.product_name + '\\n元仕入価格: ¥' + $json.original_price + '\\n許容最大価格: ¥' + $json.max_price + '\\n検索候補: ' + JSON.stringify($json.search_terms) + '\\n仕入先候補: ' + $json.suppliers.map(s => s.name).join(', ') + '\\n\\n出力形式: {\"search_queries\":[{\"supplier\":\"yahoo_auction\",\"query\":\"検索クエリ\",\"priority\":1}],\"recommendation\":\"探索すべき or 代替困難\",\"reasoning\":\"判断理由\"}' }) }}",
        "options": {
          "timeout": 30000
        }
      },
      "id": "ai-planner",
      "name": "Gemini: 検索プランナー",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "// AI検索計画をパース\nlet ai = {};\ntry {\n  const text = $input.first().json.text || $input.first().json.response || '{}';\n  ai = JSON.parse(text.replace(/```json\\n?|```/g, ''));\n} catch (e) { ai = { recommendation: '代替困難', reasoning: 'AI出力パース失敗' }; }\n\nconst context = $node['検索準備'].json;\n\nreturn [{ json: {\n  user_id: context.user_id,\n  product_id: context.product_id,\n  updated_at: context.updated_at,\n  product_name: context.product_name,\n  search_queries: ai.search_queries || [],\n  recommendation: ai.recommendation || '代替困難',\n  reasoning: ai.reasoning || '不明'\n} }];"
      },
      "id": "parse-ai",
      "name": "AI計画パース",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "conditions": {
          "string": [
            {
              "value1": "={{ $json.recommendation }}",
              "value2": "探索すべき"
            }
          ]
        }
      },
      "id": "should-search",
      "name": "探索する？",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "UPDATE products_master SET source_search_status = 'searching', source_search_queries = $1::jsonb, updated_at = NOW() WHERE id = $2 AND user_id = $3 AND updated_at = $4",
        "options": {
          "queryParams": "={{ [{{ JSON.stringify($json.search_queries).replace(/'/g, \"''\") }}, {{ $json.product_id }}, {{ $json.user_id }}, {{ $json.updated_at }}] }}"
        }
      },
      "id": "update-searching",
      "name": "検索ステータス更新（楽観的ロック）",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      }
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.N8N_WEBHOOK_URL || '={{ $vars.API_BASE_URL || $env.N8N_WEBHOOK_URL }}/webhook' }}/scrape-suppliers",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-N3-Signature",
              "value": "={{ $env.N8N_INTERNAL_TOKEN || \"\" }}"
            },
            {
              "name": "X-N3-Token-Timestamp",
              "value": "={{ Date.now() }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ user_id: $json.user_id, product_id: $json.product_id, search_queries: $json.search_queries }) }}",
        "options": {
          "timeout": 120000
        }
      },
      "id": "trigger-scraping",
      "name": "スクレイピング起動（内部認証）",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "UPDATE products_master SET source_search_status = 'no_alternative', source_search_reason = $1, updated_at = NOW() WHERE id = $2 AND user_id = $3 AND updated_at = $4",
        "options": {
          "queryParams": "={{ [{{ $json.reasoning.replace(/'/g, \"''\") }}, {{ $json.product_id }}, {{ $json.user_id }}, {{ $json.updated_at }}] }}"
        }
      },
      "id": "mark-no-alternative",
      "name": "代替なし記録（楽観的ロック）",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const data = $input.first().json;\nconst context = $node['AI計画パース'].json;\nreturn [{ json: { user_id: context.user_id, product_id: context.product_id, product_name: context.product_name, recommendation: context.recommendation, reasoning: context.reasoning, ts: new Date().toISOString() } }];"
      },
      "id": "aggregate",
      "name": "結果集約",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.chatwork.com/v2/rooms/{{ $env.CHATWORK_ROOM_ID }}/messages",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-ChatWorkToken",
              "value": "={{ $env.CHATWORK_API_KEY }}"
            }
          ]
        },
        "sendBody": true,
        "bodyParameters": {
          "parameters": [
            {
              "name": "body",
              "value": "=[info][title]🔍 仕入先探索{{ $json.recommendation === '探索すべき' ? '開始' : '断念' }}[/title]商品ID: {{ $json.product_id }}\\n商品名: {{ $json.product_name }}\\n\\n{{ $json.recommendation === '探索すべき' ? '代替仕入先の探索を開始しました。' : '代替仕入先が見つかる可能性が低いため断念。' }}\\n\\n理由: {{ $json.reasoning }}[/info]"
            }
          ]
        }
      },
      "id": "notify",
      "name": "ChatWork通知",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ success: true, product_id: $json.product_id, recommendation: $json.recommendation }) }}",
        "options": {
          "responseCode": 200
        }
      },
      "id": "respond",
      "name": "応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1
    },
    {
      "parameters": {
        "jsCode": "// 🔐 HMAC SHA256 署名検証 (Empire OS v6)\nconst crypto = require('crypto');\n\nconst signature = $request.headers['x-n3-signature'] || $request.headers['X-N3-Signature'];\nconst timestamp = $request.headers['x-n3-timestamp'] || $request.headers['X-N3-Timestamp'];\nconst secret = $env.N8N_HMAC_SECRET;\n\nif (!secret) {\n  throw new Error('🚫 N8N_HMAC_SECRET 環境変数が未設定です');\n}\n\nif (!signature || !timestamp) {\n  throw new Error('🚫 署名またはタイムスタンプが欠落しています');\n}\n\n// タイムスタンプ有効期限チェック (5分)\nconst now = Date.now();\nconst tsAge = now - parseInt(timestamp);\nif (tsAge > 300000 || tsAge < -30000) {\n  throw new Error('🚫 タイムスタンプが期限切れまたは不正です');\n}\n\n// HMAC検証\nconst bodyString = JSON.stringify($json);\nconst computedHmac = crypto\n  .createHmac('sha256', secret)\n  .update(timestamp + '.' + bodyString)\n  .digest('hex');\n\nif (signature !== computedHmac) {\n  throw new Error('🚫 HMAC署名検証失敗: 不正なリクエスト');\n}\n\n// 検証成功 - 元データをそのまま返す\nreturn [{ json: $json }];"
      },
      "name": "🔐 HMAC署名検証",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[EMPIRE_OS_V6] セキュリティ強制挿入"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{$env.SUPABASE_URL}}/rest/v1/execution_logs",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "apikey",
              "value": "={{$env.SUPABASE_ANON_KEY}}"
            },
            {
              "name": "Authorization",
              "value": "=Bearer {{$env.SUPABASE_SERVICE_ROLE_KEY}}"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Prefer",
              "value": "return=minimal"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\n  \"workflow_id\": \"{{$workflow.id}}\",\n  \"workflow_name\": \"{{$workflow.name}}\",\n  \"status\": \"success\",\n  \"execution_time_ms\": {{Date.now() - $execution.startedAt.getTime()}},\n  \"error_message\": null,\n  \"executed_at\": \"{{new Date().toISOString()}}\",\n  \"node_count\": {{$workflow.nodes.length}},\n  \"metadata\": {}\n}"
      },
      "name": "📊 実行ログ送信 (成功)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true,
      "notes": "[EMPIRE_OS_V6] 中央監視パルス"
    }
  ],
  "connections": {
    "Webhook: 仕入先切替": {
      "main": [
        [
          {
            "node": "🔐 HMAC署名検証",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "リクエスト検証": {
      "main": [
        [
          {
            "node": "エラーチェック",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "エラーチェック": {
      "main": [
        [
          {
            "node": "エラー応答",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "商品情報取得（user_id検証）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "商品情報取得（user_id検証）": {
      "main": [
        [
          {
            "node": "検索準備",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "検索準備": {
      "main": [
        [
          {
            "node": "Gemini: 検索プランナー",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Gemini: 検索プランナー": {
      "main": [
        [
          {
            "node": "AI計画パース",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "AI計画パース": {
      "main": [
        [
          {
            "node": "探索する？",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "探索する？": {
      "main": [
        [
          {
            "node": "検索ステータス更新（楽観的ロック）",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "代替なし記録（楽観的ロック）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "検索ステータス更新（楽観的ロック）": {
      "main": [
        [
          {
            "node": "スクレイピング起動（内部認証）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "スクレイピング起動（内部認証）": {
      "main": [
        [
          {
            "node": "結果集約",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "代替なし記録（楽観的ロック）": {
      "main": [
        [
          {
            "node": "結果集約",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "結果集約": {
      "main": [
        [
          {
            "node": "ChatWork通知",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "ChatWork通知": {
      "main": [
        [
          {
            "node": "応答",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🔐 HMAC署名検証": {
      "main": [
        [
          {
            "node": "リクエスト検証",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionTimeout": 300,
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "none"
  },
  "tags": [
    {
      "name": "N3-AI",
      "color": "#7b68ee"
    },
    {
      "name": "V4-PRODUCTION",
      "color": "#ff0000"
    },
    {
      "name": "Sourcing",
      "color": "#ff6347"
    },
    {
      "name": "Multi-Tenant",
      "color": "#00bfff"
    },
    {
      "name": "Internal-Auth",
      "color": "#dc143c"
    },
    {
      "name": "Empire-OS-V6",
      "color": "#ff4500"
    }
  ],
  "active": false
}