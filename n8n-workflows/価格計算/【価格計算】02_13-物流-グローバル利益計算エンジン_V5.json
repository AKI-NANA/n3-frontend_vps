{
  "name": "【価格計算】02_13-物流-グローバル利益計算エンジン_V5",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "global-profit-calc",
        "options": {
          "responseMode": "responseNode"
        }
      },
      "id": "webhook",
      "name": "Webhook: global-profit-calc",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "webhookId": "global-profit-calc"
    },
    {
      "parameters": {
        "jsCode": "const body = $input.first().json.body || $input.first().json || {};\nconst headers = $input.first().json.headers || {};\nconst apiUrl = $env.N3_API_URL || '{{$env.GATEWAY_URL}}';\nconst secret = $env.N8N_INTERNAL_SECRET;\nif (!secret) return [{ json: { error: true, message: 'N8N_INTERNAL_SECRET未設定', code: 'CONFIG_ERROR' } }];\nconst internalToken = headers['x-n3-signature'] || headers['X-N3-Signature'];\nconst tokenTimestamp = headers['x-n3-token-timestamp'] || headers['X-N3-Token-Timestamp'];\nif (internalToken && tokenTimestamp) {\n  try {\n    const r = await fetch(`${apiUrl}/api/n8n-auth/verify-internal-token`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ userId: 'system', timestamp: tokenTimestamp, token: internalToken }) });\n    if (!(await r.json()).valid) return [{ json: { error: true, message: '内部トークン無効', code: 'INVALID_TOKEN' } }];\n  } catch (e) { return [{ json: { error: true, message: 'トークン検証エラー', code: 'VERIFY_ERROR' } }]; }\n}\nconst productIds = body.product_ids || [];\nconst sourceCountry = body.source_country || 'JP';\nconst destCountry = body.dest_country || 'US';\nconst updatePrices = body.update_prices !== false;\nif (productIds.length === 0) return [{ json: { error: true, message: 'product_ids が必要です', code: 'MISSING_PARAM' } }];\nreturn [{ json: { product_ids: productIds, source_country: sourceCountry, dest_country: destCountry, update_prices: updatePrices, api_url: apiUrl, request_timestamp: new Date().toISOString() } }];"
      },
      "id": "init",
      "name": "🔐 初期化",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict"
          },
          "conditions": [
            {
              "id": "error-check",
              "leftValue": "={{ $json.error }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "equals"
              }
            }
          ]
        }
      },
      "id": "check-error",
      "name": "エラーチェック",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ success: false, error: $json.message, code: $json.code }) }}",
        "options": {
          "responseCode": 400
        }
      },
      "id": "respond-error",
      "name": "エラー応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT setting_key, setting_value FROM global_settings WHERE setting_key IN ('exchange_rate_usd_jpy', 'exchange_rate_eur_jpy', 'exchange_rate_gbp_jpy', 'vat_rate_eu', 'vat_rate_uk', 'ioss_threshold', 'ebay_fee_percent', 'paypal_fee_percent')",
        "options": {}
      },
      "id": "get-global-settings",
      "name": "⚙️ グローバル設定取得",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const settings = $input.all().map(item => item.json);\nconst settingsArray = Array.isArray(settings[0]) ? settings[0] : settings;\nconst config = {};\nsettingsArray.forEach(s => { config[s.setting_key] = parseFloat(s.setting_value) || s.setting_value; });\nconfig.exchange_rate_usd_jpy = config.exchange_rate_usd_jpy || 150;\nconfig.exchange_rate_eur_jpy = config.exchange_rate_eur_jpy || 165;\nconfig.exchange_rate_gbp_jpy = config.exchange_rate_gbp_jpy || 190;\nconfig.vat_rate_eu = config.vat_rate_eu || 0.21;\nconfig.vat_rate_uk = config.vat_rate_uk || 0.20;\nconfig.ioss_threshold = config.ioss_threshold || 150;\nconfig.ebay_fee_percent = config.ebay_fee_percent || 0.13;\nconfig.paypal_fee_percent = config.paypal_fee_percent || 0.04;\nreturn [{ json: { ...$node['🔐 初期化'].json, settings: config } }];"
      },
      "id": "parse-settings",
      "name": "📋 設定解析",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT id, sku, product_name, cost_price, weight_kg, hts_code, origin_country, listing_price, listing_currency, supplier_data FROM products_master WHERE id = ANY($1::uuid[])",
        "options": {
          "queryParams": "={{ [$json.product_ids] }}"
        }
      },
      "id": "get-products",
      "name": "📦 商品データ取得",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const products = $input.all().map(item => item.json);\nconst productsArray = Array.isArray(products[0]) ? products[0] : products;\nconst config = $node['📋 設定解析'].json;\nreturn productsArray.filter(p => p && p.id).map(product => ({ json: { ...product, config: config.settings, source_country: config.source_country, dest_country: config.dest_country, update_prices: config.update_prices } }));"
      },
      "id": "expand-products",
      "name": "📋 商品展開",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "jsCode": "const p = $input.first().json;\nconst c = p.config;\nconst costJpy = parseFloat(p.cost_price) || 0;\nconst weightKg = parseFloat(p.weight_kg) || 0.5;\nconst dest = p.dest_country;\nlet exchangeRate = c.exchange_rate_usd_jpy;\nlet vatRate = 0;\nlet shippingCostJpy = 0;\nlet dutyRate = 0.05;\nif (['DE', 'FR', 'IT', 'ES', 'NL', 'BE', 'AT'].includes(dest)) {\n  exchangeRate = c.exchange_rate_eur_jpy;\n  vatRate = c.vat_rate_eu;\n  shippingCostJpy = weightKg <= 0.5 ? 1500 : (weightKg <= 1 ? 2000 : 2500 + (weightKg - 1) * 800);\n} else if (dest === 'GB') {\n  exchangeRate = c.exchange_rate_gbp_jpy;\n  vatRate = c.vat_rate_uk;\n  shippingCostJpy = weightKg <= 0.5 ? 1800 : (weightKg <= 1 ? 2300 : 2800 + (weightKg - 1) * 900);\n} else {\n  shippingCostJpy = weightKg <= 0.5 ? 1200 : (weightKg <= 1 ? 1800 : 2200 + (weightKg - 1) * 700);\n  vatRate = 0;\n}\nconst exportCostJpy = costJpy + shippingCostJpy;\nconst exportCostForeign = exportCostJpy / exchangeRate;\nconst dutyAmount = exportCostForeign * dutyRate;\nconst subtotalWithDuty = exportCostForeign + dutyAmount;\nconst vatAmount = subtotalWithDuty * vatRate;\nconst totalImportCost = subtotalWithDuty + vatAmount;\nconst currentPrice = parseFloat(p.listing_price) || 0;\nconst ebayFee = currentPrice * c.ebay_fee_percent;\nconst paypalFee = currentPrice * c.paypal_fee_percent;\nconst totalFees = ebayFee + paypalFee;\nconst netProfit = currentPrice - totalImportCost - totalFees;\nconst profitMargin = currentPrice > 0 ? (netProfit / currentPrice) : 0;\nconst minPriceFor10Percent = totalImportCost / (1 - c.ebay_fee_percent - c.paypal_fee_percent - 0.10);\nconst recommendedPrice = Math.ceil(minPriceFor10Percent * 1.05 * 100) / 100;\nreturn [{ json: { id: p.id, sku: p.sku, product_name: p.product_name, calculation: { cost_jpy: costJpy, shipping_jpy: shippingCostJpy, export_cost_jpy: exportCostJpy, exchange_rate: exchangeRate, export_cost_foreign: Math.round(exportCostForeign * 100) / 100, duty_rate: dutyRate, duty_amount: Math.round(dutyAmount * 100) / 100, vat_rate: vatRate, vat_amount: Math.round(vatAmount * 100) / 100, total_import_cost: Math.round(totalImportCost * 100) / 100, ebay_fee: Math.round(ebayFee * 100) / 100, paypal_fee: Math.round(paypalFee * 100) / 100, total_fees: Math.round(totalFees * 100) / 100 }, current_price: currentPrice, net_profit: Math.round(netProfit * 100) / 100, profit_margin: Math.round(profitMargin * 1000) / 10, recommended_price: recommendedPrice, is_profitable: profitMargin >= 0.05, update_prices: p.update_prices, dest_country: dest } }];"
      },
      "id": "calculate-profit",
      "name": "💰 利益計算",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[PYTHON_MIGRATION_CANDIDATE]"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict"
          },
          "conditions": [
            {
              "id": "should-update",
              "leftValue": "={{ $json.update_prices }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "equals"
              }
            }
          ]
        }
      },
      "id": "check-update",
      "name": "価格更新？",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "UPDATE products_master SET pricing_data = pricing_data || $1::jsonb, updated_at = NOW() WHERE id = $2 RETURNING id, sku",
        "options": {
          "queryParams": "={{ [JSON.stringify({ global_calc: $json.calculation, net_profit: $json.net_profit, profit_margin: $json.profit_margin, recommended_price: $json.recommended_price, calculated_at: new Date().toISOString() }), $json.id] }}"
        }
      },
      "id": "update-pricing",
      "name": "💾 価格データ更新",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {
        "postgres": {
          "id": "postgres-supabase",
          "name": "Supabase Postgres"
        }
      },
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "return items;"
      },
      "id": "pass-no-update",
      "name": "更新スキップ",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "jsCode": "const results = $input.all().map(i => i.json);\nconst profitableCount = results.filter(r => r.is_profitable).length;\nconst unprofitableCount = results.filter(r => !r.is_profitable).length;\nconst avgMargin = results.length > 0 ? results.reduce((sum, r) => sum + (r.profit_margin || 0), 0) / results.length : 0;\nconst unprofitableItems = results.filter(r => !r.is_profitable).map(r => ({ sku: r.sku, margin: r.profit_margin, recommended: r.recommended_price }));\nreturn [{ json: { success: true, calculated_count: results.length, profitable_count: profitableCount, unprofitable_count: unprofitableCount, average_margin: Math.round(avgMargin * 10) / 10, unprofitable_items: unprofitableItems.slice(0, 10), calculated_at: new Date().toISOString() } }];"
      },
      "id": "aggregate",
      "name": "📊 結果集計",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict"
          },
          "conditions": [
            {
              "id": "has-unprofitable",
              "leftValue": "={{ $json.unprofitable_count }}",
              "rightValue": 0,
              "operator": {
                "type": "number",
                "operation": "larger"
              }
            }
          ]
        }
      },
      "id": "check-notify",
      "name": "赤字あり？",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.chatwork.com/v2/rooms/{{ $env.CHATWORK_ROOM_ID }}/messages",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-ChatWorkToken",
              "value": "={{ $env.CHATWORK_API_KEY }}"
            }
          ]
        },
        "sendBody": true,
        "bodyParameters": {
          "parameters": [
            {
              "name": "body",
              "value": "=[info][title]⚠️ グローバル利益計算完了[/title]計算数: {{ $json.calculated_count }}件\\n✅黒字: {{ $json.profitable_count }}件\\n❌赤字: {{ $json.unprofitable_count }}件\\n平均利益率: {{ $json.average_margin }}%\\n\\n赤字商品は価格見直しが必要です[/info]"
            }
          ]
        },
        "options": {
          "retry": {
            "attempts": 3,
            "delay": 1000,
            "backoff": "exponential"
          },
          "timeout": 30000
        }
      },
      "id": "notify",
      "name": "⚠️ 赤字通知",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json) }}",
        "options": {
          "responseCode": 200
        }
      },
      "id": "respond-success",
      "name": "✅ 成功応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1
    },
    {
      "parameters": {
        "jsCode": "// 🔐 HMAC SHA256 署名検証 (Empire OS v6)\nconst crypto = require('crypto');\n\nconst signature = $request.headers['x-n3-signature'] || $request.headers['X-N3-Signature'];\nconst timestamp = $request.headers['x-n3-timestamp'] || $request.headers['X-N3-Timestamp'];\nconst secret = $env.N8N_HMAC_SECRET;\n\nif (!secret) {\n  throw new Error('🚫 N8N_HMAC_SECRET 環境変数が未設定です');\n}\n\nif (!signature || !timestamp) {\n  throw new Error('🚫 署名またはタイムスタンプが欠落しています');\n}\n\n// タイムスタンプ有効期限チェック (5分)\nconst now = Date.now();\nconst tsAge = now - parseInt(timestamp);\nif (tsAge > 300000 || tsAge < -30000) {\n  throw new Error('🚫 タイムスタンプが期限切れまたは不正です');\n}\n\n// HMAC検証\nconst bodyString = JSON.stringify($json);\nconst computedHmac = crypto\n  .createHmac('sha256', secret)\n  .update(timestamp + '.' + bodyString)\n  .digest('hex');\n\nif (signature !== computedHmac) {\n  throw new Error('🚫 HMAC署名検証失敗: 不正なリクエスト');\n}\n\n// 検証成功 - 元データをそのまま返す\nreturn [{ json: $json }];"
      },
      "name": "🔐 HMAC署名検証",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "notes": "[EMPIRE_OS_V6] セキュリティ強制挿入"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{$env.SUPABASE_URL}}/rest/v1/execution_logs",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "apikey",
              "value": "={{$env.SUPABASE_ANON_KEY}}"
            },
            {
              "name": "Authorization",
              "value": "=Bearer {{$env.SUPABASE_SERVICE_ROLE_KEY}}"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Prefer",
              "value": "return=minimal"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\n  \"workflow_id\": \"{{$workflow.id}}\",\n  \"workflow_name\": \"{{$workflow.name}}\",\n  \"status\": \"success\",\n  \"execution_time_ms\": {{Date.now() - $execution.startedAt.getTime()}},\n  \"error_message\": null,\n  \"executed_at\": \"{{new Date().toISOString()}}\",\n  \"node_count\": {{$workflow.nodes.length}},\n  \"metadata\": {}\n}",
        "options": {
          "retry": {
            "attempts": 3,
            "delay": 1000,
            "backoff": "exponential"
          },
          "timeout": 30000
        }
      },
      "name": "📊 実行ログ送信 (成功)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true,
      "notes": "[EMPIRE_OS_V6] 中央監視パルス\n[PATCHER] WARNING: このノードはどこにも接続されていません。削除または接続してください。"
    }
  ],
  "connections": {
    "Webhook: global-profit-calc": {
      "main": [
        [
          {
            "node": "🔐 HMAC署名検証",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🔐 初期化": {
      "main": [
        [
          {
            "node": "エラーチェック",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "エラーチェック": {
      "main": [
        [
          {
            "node": "エラー応答",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "⚙️ グローバル設定取得",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "⚙️ グローバル設定取得": {
      "main": [
        [
          {
            "node": "📋 設定解析",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "📋 設定解析": {
      "main": [
        [
          {
            "node": "📦 商品データ取得",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "📦 商品データ取得": {
      "main": [
        [
          {
            "node": "📋 商品展開",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "📋 商品展開": {
      "main": [
        [
          {
            "node": "💰 利益計算",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "💰 利益計算": {
      "main": [
        [
          {
            "node": "価格更新？",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "価格更新？": {
      "main": [
        [
          {
            "node": "💾 価格データ更新",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "更新スキップ",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "💾 価格データ更新": {
      "main": [
        [
          {
            "node": "📊 結果集計",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "更新スキップ": {
      "main": [
        [
          {
            "node": "📊 結果集計",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "📊 結果集計": {
      "main": [
        [
          {
            "node": "赤字あり？",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "赤字あり？": {
      "main": [
        [
          {
            "node": "⚠️ 赤字通知",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "✅ 成功応答",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "⚠️ 赤字通知": {
      "main": [
        [
          {
            "node": "✅ 成功応答",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🔐 HMAC署名検証": {
      "main": [
        [
          {
            "node": "🔐 初期化",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionTimeout": 600,
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "all"
  },
  "tags": [
    {
      "name": "N3-Intel",
      "color": "#9400d3"
    },
    {
      "name": "V5-PRODUCTION",
      "color": "#00ff00"
    },
    {
      "name": "No-Crypto",
      "color": "#9932cc"
    },
    {
      "name": "SQL-Safe",
      "color": "#32cd32"
    },
    {
      "name": "CORE-Connected",
      "color": "#ff4500"
    },
    {
      "name": "Empire-OS-V6",
      "color": "#ff4500"
    }
  ],
  "active": false
}