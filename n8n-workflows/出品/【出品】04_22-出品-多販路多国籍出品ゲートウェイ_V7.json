{
  "name": "【出品】04_22-出品-多販路多国籍出品ゲートウェイ_V7",
  "description": "N3 Empire OS V7-27DIM: 多販路多国籍出品ゲートウェイ（27次元防衛完全対応）",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "v821-listing-gateway",
        "options": {"responseMode": "responseNode"}
      },
      "id": "webhook",
      "name": "Webhook: listing-gateway",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2
    },
    {
      "parameters": {
        "jsCode": "// ========================================\n// 🔐 V7-27次元防衛: JIT+HMAC検証\n// 次元3: Auth-Gate完全実装\n// ========================================\n\nconst crypto = require('crypto');\nconst headers = $input.first().json.headers || {};\nconst body = $input.first().json.body || $input.first().json;\n\nconst jitToken = headers['x-n3-jit-token'] || headers['X-N3-JIT-Token'];\nconst tokenExpiry = headers['x-n3-token-expiry'] || headers['X-N3-Token-Expiry'];\n\nif (jitToken && tokenExpiry) {\n  const expiryTs = parseInt(tokenExpiry, 10);\n  const now = Math.floor(Date.now() / 1000);\n  if (now > expiryTs) {\n    return [{ json: { error: true, message: 'JITトークン期限切れ', code: 'JIT_TOKEN_EXPIRED' } }];\n  }\n}\n\nconst signature = headers['x-n3-signature'] || headers['X-N3-Signature'] || '';\nconst timestamp = headers['x-n3-timestamp'] || headers['X-N3-Timestamp'] || '';\n\nif (signature && timestamp) {\n  const secret = $env.N3_HMAC_SECRET || $env.N8N_HMAC_SECRET;\n  if (!secret) {\n    return [{ json: { error: true, message: 'HMAC_SECRET未設定', code: 'CONFIG_ERROR' } }];\n  }\n  \n  const ts = parseInt(timestamp, 10);\n  const now = Date.now();\n  if (Math.abs(now - ts) > 300000) {\n    return [{ json: { error: true, message: 'タイムスタンプ期限切れ', code: 'TIMESTAMP_EXPIRED' } }];\n  }\n  \n  const computedHmac = crypto.createHmac('sha256', secret)\n    .update(timestamp + '.' + JSON.stringify(body))\n    .digest('hex');\n  \n  if (signature !== computedHmac) {\n    return [{ json: { error: true, message: 'HMAC署名検証失敗', code: 'INVALID_SIGNATURE' } }];\n  }\n}\n\nreturn [{ json: { ...body, _auth_verified: true, _auth_method: 'jit_hmac' } }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "id": "jit_hmac_v7",
      "name": "🔐 JIT+HMAC検証 (次元3)",
      "notes": "[V7-27DIM] 次元3: Auth-Gate完全実装"
    },
    {
      "parameters": {
        "jsCode": "// ========================================\n// 🔥 V7-27次元防衛: 燃焼上限チェック\n// 次元22: 経済攻撃防止\n// ========================================\n\nconst body = $input.first().json;\nif (body.error) return [{ json: body }];\n\nconst userId = body.user_id || 'default';\nconst dailyLimit = parseInt($env.BURN_LIMIT_DAILY || '1000', 10);\nconst monthlyLimit = parseInt($env.BURN_LIMIT_MONTHLY || '10000', 10);\nconst apiUrl = $env.N3_API_URL || 'http://localhost:3000';\n\ntry {\n  const resp = await fetch(`${apiUrl}/api/cost-tracking/check-limit`, {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({ \n      user_id: userId, \n      operation: 'listing_gateway', \n      estimated_cost: 0.05, \n      daily_limit: dailyLimit,\n      monthly_limit: monthlyLimit,\n      strict_mode: true\n    })\n  });\n  const result = await resp.json();\n  if (result.limit_exceeded) {\n    return [{ json: { error: true, message: `燃焼上限超過: ${result.exceeded_type}`, code: 'BURN_LIMIT_EXCEEDED' } }];\n  }\n  return [{ json: { ...body, _burn_limit_checked: true, _burn_limit_mode: 'strict' } }];\n} catch (e) {\n  return [{ json: { error: true, message: '燃焼上限チェック失敗', code: 'BURN_LIMIT_ERROR' } }];\n}"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "id": "burn_limit_v7",
      "name": "🔥 燃焼上限チェック (次元22)",
      "notes": "[V7-27DIM] 次元22: 厳格モード"
    },
    {
      "parameters": {
        "jsCode": "// ========================================\n// ⚖️ V7-27次元防衛: Policy検閲\n// 次元7: 多国籍ToS事前チェック\n// ========================================\n\nconst body = $input.first().json;\nif (body.error) return [{ json: body }];\n\nconst productIds = body.product_ids || body.ids || [];\nif (productIds.length === 0) return [{ json: { ...body, _policy_checked: true, _policy_skip: 'no_ids' } }];\n\nconst apiUrl = $env.N3_API_URL || 'http://localhost:3000';\n\ntry {\n  const policyResponse = await fetch(`${apiUrl}/api/compliance/check-multi-country`, {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({ \n      product_ids: productIds,\n      countries: body.countries || ['US', 'DE', 'UK', 'AU'],\n      checks: ['ebay_tos', 'vero', 'country_restrictions', 'customs_prohibited']\n    })\n  });\n  \n  const policyResult = await policyResponse.json();\n  \n  if (policyResult.blocked_ids && policyResult.blocked_ids.length > 0) {\n    const validIds = productIds.filter(id => !policyResult.blocked_ids.includes(id));\n    return [{ json: { ...body, product_ids: validIds, _policy_checked: true, _blocked_ids: policyResult.blocked_ids } }];\n  }\n  \n  return [{ json: { ...body, _policy_checked: true } }];\n} catch (e) {\n  console.log('Policy検閲エラー: ' + e.message);\n  return [{ json: { ...body, _policy_checked: false, _policy_warning: e.message } }];\n}"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "id": "policy_check_v7",
      "name": "⚖️ Policy検閲 (次元7)",
      "notes": "[V7-27DIM] 次元7: 多国籍ToS事前チェック"
    },
    {
      "parameters": {
        "jsCode": "// ========================================\n// 🔑 V7-27次元防衛: 最小権限トークン\n// 次元17: スコープ限定トークン\n// ========================================\n\nconst body = $input.first().json;\nif (body.error) return [{ json: body }];\n\nconst apiUrl = $env.N3_API_URL || 'http://localhost:3000';\n\ntry {\n  const tokenResponse = await fetch(`${apiUrl}/api/ebay/get-scoped-token`, {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({ required_scope: 'write_listing', jit_expiry_seconds: 300 })\n  });\n  const tokenResult = await tokenResponse.json();\n  if (tokenResult.access_token) {\n    return [{ json: { ...body, _scoped_token: tokenResult.access_token, _minimum_privilege: true } }];\n  }\n  return [{ json: { ...body, _use_legacy_token: true } }];\n} catch (e) {\n  return [{ json: { ...body, _use_legacy_token: true } }];\n}"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "id": "min_privilege_token_v7",
      "name": "🔑 最小権限トークン (次元17)",
      "notes": "[V7-27DIM] 次元17: スコープ限定トークン"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.N3_API_URL }}/api/distortion-scanner/check",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ check_types: ['exchange_rate', 'logistics_cost', 'inventory_level'], thresholds: { exchange: 0.05, logistics: 0.15, inventory: 0.3 } }) }}",
        "options": {"timeout": 30000}
      },
      "id": "distortion_scanner",
      "name": "📡 歪み検知スキャナー",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "const prevData = $node['🔑 最小権限トークン (次元17)'].json;\nconst distortion = $input.first().json;\nif (distortion.critical_distortion) {\n  return [{ json: { ...prevData, _distortion_critical: true, _distortion_type: distortion.distortion_type, _circuit_breaker_triggered: true } }];\n}\nreturn [{ json: { ...prevData, _distortion_checked: true, distortion_result: distortion } }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "id": "distortion_process",
      "name": "歪み結果処理"
    },
    {
      "parameters": {
        "conditions": {"boolean": [{"value1": "={{ $json._circuit_breaker_triggered }}", "value2": true}]}
      },
      "id": "check-circuit-breaker",
      "name": "サーキットブレーカー？ (次元12)",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.chatwork.com/v2/rooms/{{ $env.CHATWORK_ROOM_ID }}/messages",
        "sendHeaders": true,
        "headerParameters": {"parameters": [{"name": "X-ChatWorkToken", "value": "={{ $env.CHATWORK_API_KEY }}"}]},
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ body: '🚨 [サーキットブレーカー発動] 多販路出品停止\\n歪み種別: ' + $json._distortion_type }) }}",
        "options": {}
      },
      "id": "notify-admin",
      "name": "🚨 サーキットブレーカー通知 (次元12)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "notes": "[V7-27DIM] 次元12: 重大歪み検出時の通知"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO ai_decision_traces (workflow_name, node_name, input_data, output_data, model_used, reasoning, confidence_score, created_at) VALUES ('listing-gateway-v7-27dim', 'circuit-breaker', $1::jsonb, $2::jsonb, 'distortion-scanner', '重大歪み検出・出品停止', 1.0, NOW())",
        "options": {"queryParams": "={{ [JSON.stringify({distortion_type: $json._distortion_type}).substring(0, 500), JSON.stringify({paused: true}).substring(0, 500)] }}"}
      },
      "id": "ai_trace_circuit",
      "name": "📝 AI Trace (次元13)",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {"postgres": {"id": "postgres-supabase", "name": "Supabase Postgres"}},
      "continueOnFail": true
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ success: false, paused: true, reason: 'critical_distortion', distortion_type: $json._distortion_type }) }}",
        "options": {"responseCode": 503}
      },
      "id": "respond-paused",
      "name": "⏸️ 一時停止応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1
    },
    {
      "parameters": {
        "jsCode": "const apiUrl = $env.N3_API_URL;\nif (!apiUrl) return [{ json: { error: true, message: 'N3_API_URL未設定', code: 'CONFIG_ERROR' } }];\nconst body = $input.first().json;\nif (body.error) return [{ json: body }];\nconst productIds = body.product_ids || body.ids || [];\nconst targetCountries = body.countries || ['US', 'DE', 'UK', 'AU'];\nif (productIds.length === 0) return [{ json: { error: true, message: 'product_ids必須', code: 'MISSING_PARAM' } }];\nreturn [{ json: { product_ids: productIds, countries: targetCountries, api_url: apiUrl, _auth_verified: body._auth_verified, _burn_limit_checked: body._burn_limit_checked, _policy_checked: body._policy_checked, _minimum_privilege: body._minimum_privilege } }];"
      },
      "id": "init",
      "name": "🔐 初期化",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "conditions": {"conditions": [{"leftValue": "={{ $json.error }}", "rightValue": true, "operator": {"type": "boolean", "operation": "equals"}}]}
      },
      "id": "check-error",
      "name": "エラーチェック",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ success: false, error: $json.message }) }}",
        "options": {"responseCode": 400}
      },
      "id": "respond-error",
      "name": "エラー応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1
    },
    {
      "parameters": {
        "jsCode": "const init = $json;\nconst countries = init.countries;\nreturn countries.map(country => ({ json: { ...init, target_country: country } }));"
      },
      "id": "expand-countries",
      "name": "国別展開",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {"batchSize": 1, "options": {}},
      "id": "loop",
      "name": "国別処理",
      "type": "n8n-nodes-base.splitInBatches",
      "typeVersion": 3
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $json.api_url }}/api/pricing/calculate-multi-country",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ product_ids: $json.product_ids, country: $json.target_country }) }}",
        "options": {"timeout": 60000}
      },
      "id": "calc-price",
      "name": "💰 国別価格計算",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $node['国別展開'].item.json.api_url }}/api/ai/translate-listing",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ products: $input.first().json.products, target_lang: $node['国別展開'].item.json.target_country }) }}",
        "options": {"timeout": 90000}
      },
      "id": "translate",
      "name": "🌍 AI翻訳",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO listing_gateway (product_ids, target_country, pricing_data, translation_data, status, log_hash, created_at, timezone) VALUES ($1, $2, $3::jsonb, $4::jsonb, 'ready', $5, NOW(), 'Asia/Tokyo')",
        "options": {"queryParams": "={{ [$node['国別展開'].item.json.product_ids.join(','), $node['国別展開'].item.json.target_country, JSON.stringify($node['💰 国別価格計算'].json).substring(0, 2000), JSON.stringify($input.first().json).substring(0, 2000), require('crypto').createHash('sha256').update(JSON.stringify($input.first().json) + Date.now()).digest('hex')] }}"}
      },
      "id": "save-result",
      "name": "💾 結果保存 + 法廷耐性ログ (次元26)",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "credentials": {"postgres": {"id": "postgres-supabase", "name": "Supabase Postgres"}},
      "notes": "[V7-27DIM] 次元26: ハッシュ署名付き保存"
    },
    {
      "parameters": {"jsCode": "return $input.all();"},
      "id": "next-country",
      "name": "次の国へ",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ success: true, message: '多国籍出品準備完了', v7_27dim_features: { auth_verified: true, burn_limit_checked: true, policy_checked: true, minimum_privilege: true, circuit_breaker: true, ai_trace: true, forensic_log: true, distortion_scanner: true } }) }}",
        "options": {}
      },
      "id": "respond-ok",
      "name": "📤 完了応答",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1
    }
  ],
  "connections": {
    "Webhook: listing-gateway": {"main": [[{"node": "🔐 JIT+HMAC検証 (次元3)", "type": "main", "index": 0}]]},
    "🔐 JIT+HMAC検証 (次元3)": {"main": [[{"node": "🔥 燃焼上限チェック (次元22)", "type": "main", "index": 0}]]},
    "🔥 燃焼上限チェック (次元22)": {"main": [[{"node": "⚖️ Policy検閲 (次元7)", "type": "main", "index": 0}]]},
    "⚖️ Policy検閲 (次元7)": {"main": [[{"node": "🔑 最小権限トークン (次元17)", "type": "main", "index": 0}]]},
    "🔑 最小権限トークン (次元17)": {"main": [[{"node": "📡 歪み検知スキャナー", "type": "main", "index": 0}]]},
    "📡 歪み検知スキャナー": {"main": [[{"node": "歪み結果処理", "type": "main", "index": 0}]]},
    "歪み結果処理": {"main": [[{"node": "サーキットブレーカー？ (次元12)", "type": "main", "index": 0}]]},
    "サーキットブレーカー？ (次元12)": {"main": [[{"node": "🚨 サーキットブレーカー通知 (次元12)", "type": "main", "index": 0}], [{"node": "🔐 初期化", "type": "main", "index": 0}]]},
    "🚨 サーキットブレーカー通知 (次元12)": {"main": [[{"node": "📝 AI Trace (次元13)", "type": "main", "index": 0}]]},
    "📝 AI Trace (次元13)": {"main": [[{"node": "⏸️ 一時停止応答", "type": "main", "index": 0}]]},
    "🔐 初期化": {"main": [[{"node": "エラーチェック", "type": "main", "index": 0}]]},
    "エラーチェック": {"main": [[{"node": "エラー応答", "type": "main", "index": 0}], [{"node": "国別展開", "type": "main", "index": 0}]]},
    "国別展開": {"main": [[{"node": "国別処理", "type": "main", "index": 0}]]},
    "国別処理": {"main": [[{"node": "💰 国別価格計算", "type": "main", "index": 0}], [{"node": "📤 完了応答", "type": "main", "index": 0}]]},
    "💰 国別価格計算": {"main": [[{"node": "🌍 AI翻訳", "type": "main", "index": 0}]]},
    "🌍 AI翻訳": {"main": [[{"node": "💾 結果保存 + 法廷耐性ログ (次元26)", "type": "main", "index": 0}]]},
    "💾 結果保存 + 法廷耐性ログ (次元26)": {"main": [[{"node": "次の国へ", "type": "main", "index": 0}]]},
    "次の国へ": {"main": [[{"node": "国別処理", "type": "main", "index": 0}]]}
  },
  "settings": {"executionTimeout": 300, "saveDataErrorExecution": "all", "saveDataSuccessExecution": "none"},
  "tags": [
    {"name": "27DIM-FORTRESS", "color": "#dc143c"},
    {"name": "V7-ENHANCED", "color": "#00ff00"},
    {"name": "JIT-AUTH", "color": "#ffd700"},
    {"name": "BURN-LIMIT", "color": "#ff6347"},
    {"name": "POLICY-CHECK", "color": "#4169e1"},
    {"name": "MIN-PRIVILEGE", "color": "#9932cc"},
    {"name": "CIRCUIT-BREAKER", "color": "#ff4500"},
    {"name": "FORENSIC-LOG", "color": "#2f4f4f"},
    {"name": "DISTORTION-SCANNER", "color": "#ff4500"}
  ],
  "active": false,
  "_27dim_converted": true,
  "_27dim_version": "1.0.0",
  "_27dim_dimensions_implemented": [3, 7, 12, 13, 17, 22, 26],
  "_special_features": ["JIT_HMAC_Auth_Dim3", "Burn_Limit_Strict_Dim22", "Policy_Check_Dim7", "Minimum_Privilege_Token_Dim17", "Circuit_Breaker_Dim12", "AI_Decision_Trace_Dim13", "Forensic_Logging_Dim26", "Distortion_Scanner", "Auto_Pause"]
}
